# 2024 年中国研究生数学建模竞赛 C 题分析报告

## 1. 题目与材料范围

题目：**数据驱动下磁性元件的磁芯损耗建模**。

题目正文已根据公开题目页面核对；该正文说明了实验背景、斯坦麦茨方程、附件字段、问题 1～5 和提交要求。当前项目目录未保存题目 PDF/Word 原件，但已保存全部数据附件。公开核对来源：

- [公开题目正文](https://www.scribd.com/document/794992147/%E6%95%B0%E6%8D%AE%E9%A9%B1%E5%8A%A8%E4%B8%8B%E7%A3%81%E6%80%A7%E5%85%83%E4%BB%B6%E7%9A%84%E7%A3%81%E8%8A%AF%E6%8D%9F%E8%80%97%E5%BB%BA%E6%A8%A1)
- [公开数据与代码仓库](https://github.com/Tereaslle/2024-CPGMCM)
- [官方竞赛信息页面](https://cpipc.acge.org.cn/cw/contestNews/detail/4/2c908017916fade8019173a0f67f1adf?page=4)

### 1.1 附件清单与读取结果

| 材料 | 当前文件 | 结构 | 主要用途 |
|---|---|---|---|
| 附件一材料 1 | `data/raw/2024_C题/appendix1_m1.csv` | 3400 条训练记录，1028 列 | 材料 1 |
| 附件一材料 2 | `data/raw/2024_C题/appendix1_m2.csv` | 3000 条训练记录，1028 列 | 材料 2 |
| 附件一材料 3 | `data/raw/2024_C题/appendix1_m3.csv` | 3200 条训练记录，1028 列 | 材料 3 |
| 附件一材料 4 | `data/raw/2024_C题/appendix1_m4.csv` | 2800 条训练记录，1028 列 | 材料 4 |
| 附件二 | `data/raw/2024_C题/附件二（测试集）.xlsx` | 80 条测试记录，1028 列 | 预测励磁波形 |
| 附件三 | `data/raw/2024_C题/附件三（测试集）.xlsx` | 400 条测试记录，1029 列 | 预测磁芯损耗 |
| 附件四 | `data/raw/2024_C题/附件四（Excel表）.xlsx` | Sheet1 为 400 行、3 列结果表 | 提交问题 1、4 结果 |

附件一每条记录的字段为：温度、频率、磁芯损耗、励磁波形、1024 个磁通密度采样值。附件二没有励磁波形标签，需要分类；附件三已有励磁波形标签，但没有磁芯损耗，需要回归预测。附件四 Sheet2、Sheet3 为空白模板页，后续写入时应保留，不要擅自删除。

### 1.2 数据质量与范围

- 训练数据合计 12400 条；每条波形有 1024 个等间隔采样点。
- 温度取 25、50、70、90 °C。
- 频率题目给出的总体范围为 50000～500000 Hz；实际材料文件的频率覆盖并不完全相同，优化时应使用材料相关的经验可行域，避免无依据外推。
- 励磁波形为正弦波、三角波、梯形波。
- 磁芯材料为材料 1～材料 4。
- 训练集和两个测试集均未发现缺失单元格。
- 训练损耗跨度很大，后续回归更适合检查对数损耗尺度；这只是建模建议，不是已求出的结论。
- 附件二按材料各 20 条，共 80 条；附件三按材料各 100 条，共 400 条。
- 附件四 Sheet1 的第 2 列用于填附件二的波形分类，第 3 列用于填附件三的损耗预测。

对波形原始序列建议同时保留两类量：峰值磁通密度 (B_m) 与峰峰值 ΔB。题目中的斯坦麦茨方程使用峰值；非正弦波分析和 iGSE 类思想更常使用峰峰值或磁通密度变化率，二者不能混用。

## 2. 五个问题的总体判断

| 问题 | 通俗任务 | 主要类型 | 直接输出 |
|---|---|---|---|
| 1 | 看 1024 个磁通密度点，判断它是正弦、三角还是梯形 | 多分类、信号特征识别 | 附件二 80 条波形标签、附件四第 2 列、三类数量和指定样本结果 |
| 2 | 检查经典斯坦麦茨公式在不同温度下的误差，并把温度加进公式 | 参数拟合、回归建模、模型比较 | 原 SE 参数、温度修正公式、误差对比与适用范围 |
| 3 | 研究温度、波形、材料分别及两两组合如何影响损耗，并找低损耗条件 | 统计分析、方差分析、解释性回归 | 主效应、两两交互效应、影响程度、低损耗条件 |
| 4 | 用训练数据建立一个跨温度、频率、波形、材料的损耗预测器 | 回归预测、监督学习 | 附件三 400 条预测值、附件四第 3 列、指定样本预测值、精度与泛化评估 |
| 5 | 在损耗尽量小的同时，让传输磁能指标 (fB_m) 尽量大 | 多目标优化、离散—连续混合优化 | Pareto 解集、损耗最小方案、传输磁能最大方案和折中方案 |

## 3. 问题 1：励磁波形分类

### 3.1 它究竟要求什么

附件一给出了带真实波形标签的训练曲线。需要从磁通密度随时间变化的 1024 个点中提取形状信息，训练一个三分类模型，再识别附件二 80 个无标签样本。最终不是只报告准确率，而是必须把 80 个类别编号写入附件四，并在论文中统计三类数量、列出指定 10 个样本的类别。

类别编码由题目固定：1 为正弦波，2 为三角波，3 为梯形波。

### 3.2 输入、已知条件与输出

| 项目 | 内容 |
|---|---|
| 输入 | 附件一四种材料的 1024 点磁通密度曲线及波形标签；附件二的温度、频率、材料和 1024 点曲线 |
| 已知条件 | 训练标签为三类；采样点顺序代表一个周期内等间隔时间顺序；材料、温度和频率可作为辅助特征 |
| 输出 | 附件二每个样本的 1/2/3 分类编号；三类数量；样本 1、5、15、25、35、45、55、65、75、80 的结果 |
| 必须提交 | 保持附件四原文件名，填写第 2 列并作为附件上传；正文给出分类方法、验证指标和指定结果表 |
| 验证 | 分层交叉验证、混淆矩阵、宏平均 F1；检查材料和温度分组下是否仍稳定 |

### 3.3 分层方案

- **基础可行方案**：对每条曲线提取均值、标准差、峰峰值、偏度、峰度、最大斜率、斜率分布、平台比例、谐波能量比等形状特征，用决策树或多分类逻辑回归完成三分类。优点是快、易解释；缺点是阈值和特征选择可能影响边界样本。
- **比赛推荐方案**：形状特征 + 随机森林为主模型，必要时以 RBF-SVM 作对比。随机森林对非线性边界、特征量纲和少量噪声较稳健，且可报告特征重要性。验证必须只在训练集内完成，不能用附件二反向调参。
- **时间充裕的进阶方案**：原始 1024 点序列的一维卷积网络，或“原始曲线编码 + 手工特征”的融合模型。只有当交叉验证明确优于推荐方案时才值得采用；否则不建议为追求高级而增加风险。

### 3.4 主要风险

不能把附件二的温度、频率或材料当成波形标签的替代品；分类依据应主要来自波形形状。还要防止把同一条曲线的采样点随机拆开造成数据泄漏，正确做法是以“整条曲线”为一个样本。

## 4. 问题 2：温度修正的斯坦麦茨方程

### 4.1 它究竟要求什么

传统斯坦麦茨方程在正弦励磁下写作

\[
P_v=k f^{\alpha}B_m^{\beta}.
\]

它没有显式温度项。题目要求在同一种材料、正弦波形条件下，利用附件一材料 1 的数据，构造能适应不同温度的修正方程，并与未修正的 SE 比较预测误差。

### 4.2 输入、已知条件与输出

| 项目 | 内容 |
|---|---|
| 输入 | 附件一材料 1 中波形为正弦波的记录：温度 (T)、频率 (f)、磁通密度曲线、实测损耗 (P_v) |
| 已知条件 | 温度只有四个实验水平；SE 的基本变量为 (f) 和 (B_m)；(k,α,β) 需要从数据估计 |
| 输出 | 原始 SE 参数；温度修正后的方程及参数；两者在材料 1 正弦数据上的误差比较；适用范围和不足 |
| 必须提交 | 正文中的公式、参数表、误差指标、残差或分组误差图；题目没有要求另交 Excel 结果 |
| 验证 | 留出集或按温度分组交叉验证；比较 RMSE、MAE、相对误差和各温度层误差 |

### 4.3 分层方案

- **基础可行方案**：对每个温度分别拟合
  \(\ln P_v=\ln k+\alpha\ln f+\beta\ln B_m\)，得到四组 SE 参数，再与一组不区分温度的 SE 比较。它能直接展示温度影响，但参数较多，跨温度泛化有限。
- **比赛推荐方案**：建立统一的温度扩展 SE，对数形式可写为
  \[
  \ln P_v=c+\alpha\ln f+\beta\ln B_m+g(T),
  \]
  其中 (g(T)) 先用线性或二次温度项表示；若残差显示温度改变了频率或磁通密度指数，再有依据地加入 (T\ln f)、(T\ln B_m) 交互项。把未修正 SE 作为嵌套基线，用留出误差判断温度项是否真正有效。
- **时间充裕的进阶方案**：采用带正则化的非线性最小二乘、分层/混合效应 SE 或平滑温度函数。四个温度水平下不宜使用过度自由的高阶函数，进阶模型必须接受严格的留温度验证。

### 4.4 主要风险

温度只有四个水平，不能把复杂温度曲线解释成已被充分验证的物理规律。(B_m) 的定义必须固定；如果波形存在直流偏置，应同时检查峰值和峰峰值口径。所有参数都只在材料 1、正弦波、题目数据范围内解释。

## 5. 问题 3：温度、波形和材料的影响及交互作用

### 5.1 它究竟要求什么

题目不只问“哪个因素影响最大”，还要求区分：

1. 温度、波形、材料各自的独立影响；
2. 温度×波形、温度×材料、波形×材料三组两两交互影响；
3. 三者影响程度的量化；
4. 哪些条件组合可能产生最低磁芯损耗。

频率和磁通密度同样会影响损耗，因此比较三大因素时应控制或说明 (f)、(B_m) 的影响，否则可能把工况差异误判为材料或波形效应。

### 5.2 输入、已知条件与输出

| 项目 | 内容 |
|---|---|
| 输入 | 附件一四个材料文件的温度、频率、损耗、波形和 1024 点磁通密度 |
| 已知条件 | 温度 4 水平、波形 3 类、材料 4 类；训练标签完整；题目只要求两两交互，不要求三阶交互 |
| 输出 | 三个主效应、三个两两交互效应、影响程度排序、最低损耗条件及其解释 |
| 必须提交 | 正文中的统计模型、显著性/效应量表格、交互作用图、低损耗条件表和结论 |
| 验证 | 方差分析假设检查、残差诊断、对数损耗与原尺度的敏感性比较、分组或重抽样稳定性检查 |

### 5.3 分层方案

- **基础可行方案**：按温度、波形和材料分组计算平均损耗/中位数损耗，并对每一对因素做双因素方差分析，使用交互项显著性判断协同或拮抗。
- **比赛推荐方案**：以 \(\ln P_v\) 为响应变量，建立包含温度、波形、材料主效应及三组两两交互项的阶乘回归/ANOVA，同时加入 \(\ln f\)、\(\ln B_m\) 作为控制变量。用偏 \(\eta^2\)、部分 (R^2)、似然比检验或标准化贡献量比较影响程度。低损耗条件优先在观测数据支持的可行域内搜索。
- **时间充裕的进阶方案**：使用广义加性模型或分层模型描述频率、磁通密度的非线性，再用置换重要性或基于模型的边际效应解释交互作用。进阶方案不能取代清晰的主效应和交互效应表。

### 5.4 主要风险

如果直接对原始损耗做 ANOVA，极大损耗值可能主导结果；建议把对数损耗作为主分析尺度，并用原尺度结果复核。若某些温度×材料×波形组合样本很少，不能把不稳定的均值差异写成确定规律。

## 6. 问题 4：基于数据驱动的磁芯损耗预测

### 6.1 它究竟要求什么

利用附件一的有标签实验记录，建立一个能跨材料、温度、频率和波形预测磁芯损耗的回归模型，再对附件三 400 个测试样本给出预测值。题目关注的不只是预测数字，还要求说明模型精度、泛化能力和工程指导意义。

### 6.2 输入、已知条件与输出

| 项目 | 内容 |
|---|---|
| 输入 | 训练集：温度、频率、材料、励磁波形、1024 点磁通密度和实测损耗；测试集：附件三的相同输入但无损耗 |
| 已知条件 | 附件三已经给出波形标签，因此问题 4 不必等待问题 1 的分类结果 |
| 输出 | 400 条磁芯损耗预测值，保留 1 位小数；模型精度、泛化和解释；指定样本 16、76、98、126、168、230、271、338、348、379 的结果 |
| 必须提交 | 保持附件四原文件名，填写第 3 列；正文给出模型、验证指标和指定结果表 |
| 验证 | RMSE、MAE、(R^2)、相对误差；随机留出之外，还应做按工况/材料的分组验证，检查跨条件泛化 |

### 6.3 分层方案

- **基础可行方案**：以 (T,f,B_m)、材料、波形和若干统计/频域特征为输入，用对数线性回归或随机森林回归建立基线；优点是实现快、容易解释。
- **比赛推荐方案**：提取波形的时域、斜率、峰值、峰峰值、谐波和平台特征，加入温度、频率、材料、波形，使用梯度提升树（如 XGBoost）或随机森林回归；以 \(\ln P_v\) 训练，再还原到原尺度。用修正 SE 作为可解释基线，用树模型作为精度主模型，二者最多保留两个独立模型体系。
- **时间充裕的进阶方案**：采用“修正 SE 预测 + 机器学习残差”的混合模型，或一维卷积网络直接读取 1024 点序列。只有在分组验证真实提升时才采用；不得只报告随机划分的高分。

### 6.4 关键实现边界

问题 4 的模型若直接使用 1024 个采样点，问题 5 很难直接对 1024 个自由变量优化。因此，比赛推荐方案应尽量形成“可控变量—特征—损耗”的代理模型：温度、频率、波形、材料、(B_m) 加少数波形参数。若保留原始序列模型，问题 5 应通过正弦/三角/梯形波参数化生成候选曲线，再计算同样的特征，不能让优化器任意修改 1024 个点。

## 7. 问题 5：磁性元件的最优化条件

### 7.1 它究竟要求什么

题目要求同时追求两个目标：

- 磁芯损耗尽量小，使用问题 4 的预测模型作为损耗目标；
- 传输磁能指标尽量大，题目将其简化为 (fB_m) 最大。

这两个目标通常互相冲突：增大频率或磁通密度往往有利于 (fB_m)，但也可能增加损耗。因此不能只给一个“损耗最小”的点就宣称完成，应给出两个极端解和一组 Pareto 折中解。

### 7.2 输入、已知条件与输出

| 项目 | 内容 |
|---|---|
| 输入 | 问题 4 的损耗预测模型；温度、频率、波形、磁通密度峰值、材料及其经验范围 |
| 已知条件 | 温度和材料、波形是离散选择；频率、磁通密度峰值是连续或离散可调量；传输磁能指标固定为 (fB_m) |
| 输出 | 最小损耗条件、最大 (fB_m) 条件、Pareto 前沿及一个工程折中方案；每个方案的两个目标值 |
| 必须提交 | 正文中的目标函数、约束、求解方法、最优/折中条件、敏感性和可行性说明 |
| 验证 | 与训练数据中的最优观测组合比较；检查候选点是否超出数据支持域；改变权重或约束后观察方案稳定性 |

### 7.3 分层方案

- **基础可行方案**：在训练数据中枚举真实出现的工况组合，计算每个组合的损耗和 (fB_m)，筛选非支配解。这种方案几乎没有外推风险，适合先给出可靠基线。
- **比赛推荐方案**：用问题 4 的代理模型作为损耗预测器，在温度、材料、波形离散选择和 (f,B_m) 连续范围内建立混合整数多目标优化；采用归一化加权和或 ε-约束法求折中，并用 Pareto 前沿展示权衡。频率和 (B_m) 的边界优先取训练数据的经验范围，必要时加入最近邻距离或凸包约束。
- **时间充裕的进阶方案**：采用 NSGA-II 等多目标进化算法，再加入预测不确定性形成鲁棒 Pareto 优化。进阶结果必须与网格枚举和多组随机种子复核，不可只相信一次进化算法输出。

### 7.4 主要风险

如果只最小化损耗，算法可能把频率和磁通密度压到边界，无法回答“兼顾传输磁能”。如果只最大化 (fB_m)，则可能得到高损耗方案。最重要的风险是超出训练数据范围后产生看似精确但不可信的外推，因此可行域约束比算法名称更重要。

## 8. 五问之间的依赖关系

### 8.1 题目叙事流程

按题目和论文写作顺序，可以表示为：

```text
原始数据
   ↓
问题1：波形分类
   ↓
问题2：温度修正 SE
   ↓
问题3：因素与交互作用分析
   ↓
问题4：数据驱动损耗预测
   ↓
问题5：损耗—传输磁能多目标优化
```

这条链适合组织论文故事，但不等于所有计算必须严格串行。

### 8.2 实际计算依赖

```text
                         ┌→ 问题1 → 附件二波形标签 → 附件四第2列
原始数据 → 数据审计 ─────┼→ 问题2 → 温度修正 SE 基线
                         ├→ 问题3 → 因素效应与交互约束
                         └→ 问题4 → 附件三损耗预测 → 附件四第3列
                                                    ↓
                                          问题5：多目标优化
```

- 问题 1、问题 2、问题 3 可以在完成数据审计后并行。
- 问题 4 也可以独立启动，因为附件三已经提供励磁波形标签；它不必等待问题 1。
- 问题 2 可以作为问题 4 的物理基线，问题 3 可以为问题 4 和问题 5 提供变量解释与约束，但这属于增强关系，不是硬依赖。
- 问题 5 对问题 4 有硬依赖：它必须使用问题 4 的损耗预测模型作为目标函数。问题 3 的影响分析和问题 2 的 SE 可作为解释、初始化或稳健性检查。
- 附件四的两列结果可分别完成：第 2 列来自问题 1，第 3 列来自问题 4，最后统一检查样本序号、格式和小数位。

## 9. 推荐的比赛期间执行顺序

1. 统一读取四个训练文件，固定字段名、单位、波形编码、材料编码和 (B_m/\Delta B) 定义。
2. 并行完成问题 1 的分类基线、问题 2 的 SE 基线、问题 3 的分组统计和问题 4 的预测基线。
3. 优先锁定问题 1 和问题 4 的附件四结果，避免最后才处理提交文件。
4. 用问题 2 和问题 3 的结果解释问题 4 的特征和误差来源。
5. 先做问题 5 的真实工况枚举，再做代理模型连续优化；两者结论不一致时优先相信数据支持域内的结果，并解释差异。
6. 统一检查五问结论、单位、样本序号、四舍五入规则和附件文件名。

## 10. 关键假设与回退条件

### 关键假设

1. 1024 个采样点按一个周期等间隔排列，可以代表励磁波形形状。
2. 训练数据与测试数据的采集口径一致，材料 1～4 的编号在训练与测试中含义一致。
3. 问题 5 的优化只在训练数据支持的温度、频率、波形、材料和磁通密度范围内解释。

### 可放宽假设

1. 损耗响应使用对数尺度拟合；需用原尺度误差复核。
2. (B_m) 使用峰值，ΔB 用于非正弦波特征；若存在直流偏置，需同时报告两种口径的敏感性。
3. 温度修正项先取线性/二次形式；只有留出验证有效时才增加交互项。
4. 波形分类主要依赖形状特征；可加入温度、频率、材料，但必须做消融检查。
5. 预测模型可用树模型近似复杂非线性；需用 SE 基线和分组验证检查泛化。

若关键假设不成立，回退方式分别是：按真实采样周期重建曲线、重新核对材料映射、缩小优化域或只报告观测工况 Pareto 解。

## 11. 本阶段结论

这是一道“信号分类 + 经验参数拟合 + 统计解释 + 数据回归 + 多目标优化”的组合题。比赛最稳妥的主线是：问题 1 用可解释波形特征分类，问题 2 用对数线性温度修正 SE，问题 3 用控制频率和磁通密度后的阶乘 ANOVA，问题 4 用特征工程加树模型，问题 5 先观测枚举再做代理模型 Pareto 优化。高级模型只有在验证指标和稳健性都明确改善时才采用。

本报告只完成题目拆解和方案设计，不包含代码、模型训练结果或最终数值答案。
