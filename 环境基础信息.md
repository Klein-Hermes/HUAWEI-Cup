# HUAWEI-Cup 基础开发环境信息

更新时间：2026-09-11

## 项目位置

- 本地项目目录：`D:\PycharmProject\HUAWEI-Cup`
- Conda 环境名称：`huawei-cup`
- Conda 环境路径：`D:\Anaconda\envs\huawei-cup`
- Python 解释器：`D:\Anaconda\envs\huawei-cup\python.exe`

## 环境定位

这是一个面向华为杯前期准备的通用科学计算环境，适合数据清洗、统计分析、数学建模、可视化和基础机器学习。

当前没有安装 PyTorch、CUDA、MMDetection 等重型或赛道专用组件，后续根据正式赛题再补充，避免与已有深度学习环境发生冲突。

## Python 版本

| 项目 | 版本 |
|---|---|
| Python | 3.10.21 |
| Conda 环境 | `huawei-cup` |
| JupyterLab | 4.4.7 |
| Jupyter 内核 | `Python (huawei-cup)` |

## 已安装的基础库

| 库 | 版本 | 用途 |
|---|---:|---|
| NumPy | 2.2.6 | 数值计算与数组运算 |
| Pandas | 2.3.3 | 表格数据处理 |
| SciPy | 1.15.2 | 科学计算、统计与优化基础 |
| Matplotlib | 3.10.9 | 基础绘图 |
| Seaborn | 随 Matplotlib 环境安装 | 统计可视化 |
| Scikit-learn | 1.7.2 | 基础机器学习 |
| SymPy | 1.14.0 | 符号计算 |
| Statsmodels | 0.15.0 | 统计建模与回归分析 |
| OpenPyXL | 3.1.5 | Excel `.xlsx` 文件读写 |
| xlrd | 已安装 | 部分 Excel 文件读取 |
| PyYAML | 已安装 | YAML 配置文件读写 |

## 验证结果

已使用该环境完成导入测试，以下库均可正常导入：

`numpy`、`pandas`、`scipy`、`matplotlib`、`seaborn`、`sklearn`、`sympy`、`statsmodels`、`openpyxl`、`yaml`

## 使用方式

### Conda 命令行

```powershell
conda activate huawei-cup
cd D:\PycharmProject\HUAWEI-Cup
jupyter lab
```

### PyCharm

项目解释器选择：

```text
D:\Anaconda\envs\huawei-cup\python.exe
```

### 环境复现

项目中的 `environment.yml` 可用于重新创建环境：

```powershell
conda env create -f environment.yml
```

## 现有电脑中的相关环境

电脑中原有的环境没有被修改：

- `base`：Anaconda 基础环境
- `pytorch`：Python 3.10、PyTorch 2.5.1、CUDA 11.8、RTX 3060 支持
- `mmdet`：MMDetection 深度学习环境

本项目暂时不依赖上述深度学习环境。

## 后续扩展原则

根据正式赛题再按需增加：

- 优化算法：`pulp`、`ortools`、`gekko` 等
- 机器学习：`xgboost`、`lightgbm`
- 深度学习：使用独立的 PyTorch 环境或另建赛道环境
- MATLAB：仅在题目或队伍工作流确实需要时配置

不建议把所有可能的库一次性安装到当前环境中。
