# D题小规模约束审计验证方案

## 1. 验证目标

本验证的目标不是证明求解器返回了一个“最优状态”，而是主动发现以下风险：

> 复杂约束没有被正确编码，但由于目标函数有改善、求解器返回 `optimal`，误以为已经得到了原问题的优化结果。

因此，必须同时验证：

1. 方案是否满足原题全部硬约束；
2. 目标函数是否由方案数据独立重算得到；
3. 小规模实例的结果是否与精确枚举结果一致；
4. 删除某条约束后，测试是否能够检测出模型变化。

当前项目中已核验到：D题包含 80 个货箱、15 个服务区、8 架运输无人机、A/B/C 三种机型、A/B/C 电池库存分别为 6/4/4、2 架中继无人机和 6 组中继能源组件。

## 2. 默认重点验证的三类约束

由于原始需求中没有展开“三个点”的具体内容，本方案暂按 D 题最容易漏编码的三类约束处理：

### 2.1 飞行能耗、载荷变化与返航安全余量

- 货箱不可拆分；
- 质量和体积分别受限；
- 多点路线中，每次投递后剩余载荷必须更新；
- 每个航段的能耗必须使用该航段的实际剩余载荷；
- 架次总能耗必须满足返航安全余量；
- 航程、爬升、巡航、下降规则必须统一。

### 2.2 时间、无人机、电池与充电周转

- 无人机任务区间不得重叠；
- 同一块电池的任务占用和充电区间不得重叠；
- 重新执行任务前电池必须充满；
- 医疗物资和首批保障货箱必须满足时间要求；
- 货箱送达时间必须按实际到达服务区的时刻计算，而不是按架次结束时刻替代；
- 固定准备时间、装载时间、交接时间和架次周转时间不能遗漏。

### 2.3 连续通信、中继匹配与任务分区继承

- 通信状态必须覆盖爬升、巡航、下降和投送阶段；
- 不能只检查航段起点和终点；
- 中继接入链路和中继回传链路必须在同一时刻同时可用；
- 每架运输无人机在同一时刻只能由固定网关或一架中继无人机保障；
- 第4问必须继承第3问的运输与通信方案，不能偷偷重新规划路线；
- 同一架次涉及的多个服务区必须划入同一任务组；
- 不同任务组之间不得共享运输无人机、电池、中继无人机和能源组件。

## 3. 独立可行性审计器

审计器不能调用优化模型内部的约束函数，而应直接读取求解器输出的方案表和原始输入数据，重新计算以下内容：

- 每个货箱是否恰好分配一次；
- 每架次的路线、访问顺序和实际载荷；
- 每段飞行时间、能耗和剩余 SOC；
- 每个货箱的实际送达时刻；
- 无人机、电池、能源组件的占用区间；
- 每个时刻的通信状态；
- 第4问的分区和资源需求；
- 各个目标函数分量。

建议每个运输架次至少输出：

```text
架次编号、无人机编号、机型、电池编号
路线节点、各节点到达时刻、货箱编号
各航段剩余载荷、各航段能耗、架次总能耗
返航时剩余SOC、货箱送达时刻
通信状态区间、使用的中继编号
```

审计结果至少应包含：

```text
hard_violation_count
max_mass_violation
max_volume_violation
max_energy_violation
max_time_violation
communication_gap_count
resource_overlap_count
box_duplicate_count
box_missing_count
objective_recompute_error
```

任何一个硬约束违反，都应判定为 `FAIL`，不受求解器是否返回 `optimal` 影响。

## 4. 基础公式和边界单元测试

这一层不调用优化器，只测试确定性计算函数。

### 4.1 质量和体积分别测试

A 型机参数为最大载质量 25 kg、最大装载体积 0.06 m³。

构造两个独立样例：

| 样例 | 总质量 | 总体积 | 预期结果 |
|---|---:|---:|---|
| 两个饮用水箱 | 28 kg | 0.054 m³ | 质量不满足，体积满足 |
| 两个生活卫生用品箱 | 12 kg | 0.070 m³ | 质量满足，体积不满足 |

这样可以发现只检查质量、不检查体积，或者把质量和体积错误合并为一个约束的问题。

### 4.2 电池充电公式边界测试

以 A 型电池完全充电时间 (T_{mathrm{full}}=1800) s 为例：

\[
t_{\mathrm{chg}}(s)=
\begin{cases}
T_{\mathrm{full}}\left[0.65\dfrac{0.90-s}{0.90}+0.35\right], & 0\leq s<0.90,\\
T_{\mathrm{full}}\cdot0.35\dfrac{1-s}{0.10}, & 0.90\leq s\leq1.
\end{cases}
\]

应满足：

\[
t_{\mathrm{chg}}(0)=1800\text{ s},
\quad t_{\mathrm{chg}}(0.5)=1150\text{ s},
\quad t_{\mathrm{chg}}(0.9)=630\text{ s},
\quad t_{\mathrm{chg}}(1)=0.
\]

必须检查：

- (s=0.9) 时两段公式结果一致；
- SOC 小于 0 或大于 1 时拒绝；
- 充电时间不能为负；
- 电池未充满时不能直接再次执行架次。

### 4.3 返航安全余量边界测试

每个运输架次必须满足：

\[
E_{\mathrm{trip}}\leq(1-\rho_g)E_{g,\mathrm{use}}.
\]

至少构造三个样例：

1. 能耗略低于上限：必须可行；
2. 能耗等于上限：必须可行；
3. 能耗略高于上限：必须不可行。

同时测试多点路线：

```text
O01 → S1 → S2 → O01
```

S1 投递后，S1→S2 和 S2→O01 的载荷必须下降。不能用架次初始载荷计算所有航段能耗。

## 5. 小规模精确枚举

先建立 2～3 个服务区、4～8 个货箱的小实例，用穷举或独立动态规划得到精确最优结果，再与优化器比较。

| 实例 | 规模 | 主要目的 |
|---|---:|---|
| T1 | 1个服务区、3个货箱、1架无人机 | 不可拆箱、质量、体积 |
| T2 | 2个服务区、4个货箱、2架次 | 服务区不能错误混批 |
| T3 | 2个服务区、1架无人机、1组电池 | 电池充电和时间顺序 |
| T4 | 2个服务区、多点路线 | 载荷递减和航段能耗 |
| T5 | 1条被地形遮挡的航段 | 连续通信 |
| T6 | 2个任务组、共享无人机 | 第4问禁止跨组调配 |

每个小实例至少比较：

1. 求解器是否找到可行解；
2. 独立审计器是否判定可行；
3. 求解器目标值与独立重算目标值是否一致；
4. 求解器目标值是否等于精确枚举得到的最优值。

如果小实例中有一个目标值不一致，只能认为求解器优化了某个“编码后的模型”，不能认为它解决了原题模型。

## 6. 通信约束反例

### 6.1 中间时刻通信中断

构造一条航段：

```text
起点直连可用
中间某段被DEM地形遮挡
终点直连可用
```

预期：整条航段不可行。

如果程序只检查起点和终点，该方案会被错误接受。

### 6.2 中继链路不同步

构造：

```text
运输无人机—中继链路：0～100 s可用
中继—G01回传链路：20～120 s可用
```

两条链路共同可用的时间只有 20～100 s。运输无人机在共同覆盖区间之外需要通信时，方案必须不可行。

不能将两条链路的可用时间简单取并集。

### 6.3 直连、中继和中断状态

通信状态必须严格按以下优先级判定：

1. 直连可用：直连状态；
2. 直连不可用且两段中继链路同时可用：中继状态；
3. 其他情况：通信中断状态。

不能因为存在某个中继无人机，就认为运输无人机始终有通信。

通信公式还应单独测试：

- 频率单位使用 MHz；
- 距离单位使用 km；
- 双向链路取两个方向允许损耗的较小值；
- 地形遮挡附加损耗为 10 dB；
- 中继接入和中继回传使用不同设备参数。

建议固定一个 1 km 链路，人工计算自由空间传播损耗，再与程序结果比较，重点排查 MHz/Hz、m/km 和单向/双向链路错误。

## 7. 第4问任务分区验证

第4问最容易出现“偷偷重新优化”的问题。它必须继承第3问已经确定的：

- 货箱分配；
- 服务区访问顺序；
- 运输无人机和中继无人机任务；
- 架次开始时刻；
- 通信保障关系。

第4问只能改变任务组标签和独立资源核算，不能重新安排路线。

### 7.1 分区合法性

每个服务区必须恰好属于一个任务组，每个任务组至少包含一个服务区。

若某架次为：

```text
O01 → S001 → S015 → O01
```

则 S001 和 S015 必须属于同一任务组。只要分到不同组，立即判定失败。

### 7.2 资源独立性

如果问题三中同一架运输无人机先后执行两个不同任务组的任务，第4问不能继续认为只需要一架无人机。由于任务组之间不能跨组调配，两个组必须分别配置资源。

应分别计算：

1. 固定问题三资源编号得到的使用数量；
2. 固定问题三时间表下，通过区间着色得到的最少资源数量。

如果论文声称的是“独立执行所需最少资源”，必须采用第二种计算，而不能只统计问题三中出现过多少个资源编号。

## 8. 约束突变测试

对每条关键约束做一次故意删除或放宽，验证测试体系能否捕获模型变化。

建议的突变包括：

- 删除货箱体积约束；
- 删除返航安全余量；
- 删除电池充电不重叠约束；
- 删除医疗物资时间约束；
- 只检查通信端点，不检查中间时刻；
- 允许中继链路不同步；
- 删除同一架次服务区必须同组约束；
- 允许第4问重新规划第3问路线。

正确的测试结果应为：

- 原模型：对应反例不可行；
- 删除对应约束后的突变模型：反例可能变为可行；
- 独立审计器：仍能发现该违反。

如果删除一条约束后模型输出完全不变，说明该实例没有真正触发该约束，需要重新设计测试数据。

## 9. 最终通过标准

只有同时满足以下条件，才允许声称模型结果可信：

1. 所有基础公式边界测试通过；
2. T1～T6 小实例全部通过独立审计；
3. 小实例最优值与穷举结果一致；
4. 所有负例均被拒绝；
5. 每条关键约束的删除突变都能被测试捕获；
6. 实际 D 题子集上没有货箱遗漏或重复；
7. 所有架次满足能量和返航余量；
8. 所有时间窗满足；
9. 无人机、电池和能源组件没有区间重叠；
10. 连续通信没有空档；
11. 第4问没有改变第3问方案；
12. 独立重算目标值与程序报告值一致。

任何一条失败，都应报告为“模型或编码仍需修正”，而不是通过调整目标权重掩盖问题。

## 10. 一小时可执行版

### 10.1 时间边界

原方案中的全部测试项如果从零开发、完整运行，不能保证一小时内完成。因此实际执行时采用“一小时核心版”，完整测试清单作为后续扩展。

一小时核心版的前提是：

- 已经存在可调用的 D 题求解器；
- 求解器能够接收缩小后的货箱、服务区、资源和 DEM 实例；
- 求解器能够导出结构化方案；
- 基础计算函数已经可以单独调用。

如果还需要从零编写求解器、重构输出接口或开发独立审计器，一小时只能完成接口和部分单元测试，不能声称完成验证。

### 10.2 核心实例压缩

只使用一个统一的小实例，避免为每个测试重新准备数据：

- 服务区：S001、S002、S015；
- 货箱：选择 8 个，至少包含医疗物资、饮用水、应急食品和生活卫生用品；
- 运输无人机：保留 1 架 A 型、1 架 B 型和 1 架 C 型；
- 共享电池：每种机型先保留 1～2 组；
- 中继无人机：保留 1 架；
- 中继能源组件：保留 1～2 组；
- 通信测试使用一个 3×3 或 5×5 的合成 DEM，另做一次真实 DEM 点检。

这样既保留了实际参数量级，也能覆盖质量、体积、时间、能量、通信和分区约束，不运行完整的 15 服务区、80 货箱优化。

### 10.3 一小时时间表

| 时间段 | 内容 | 数量/目标 |
|---|---|---|
| 0～5 min | 读取小实例、固定随机种子、检查方案输出字段 | 1 个实例 |
| 5～15 min | 公式与边界测试 | 充电、能耗、SOC、链路预算约 8～10 个断言 |
| 15～25 min | 精确小实例与求解器比较 | T1～T4 压缩为 2～3 个实例 |
| 25～35 min | 时间和资源冲突测试 | 1架无人机、1组电池、两架次 |
| 35～45 min | 连续通信反例 | 中间遮挡、链路不同步、直连/中继切换 |
| 45～53 min | 第4问分区继承测试 | 2个任务组、1条跨服务区架次 |
| 53～58 min | 关键约束突变测试 | 至少删除 4 条高风险约束 |
| 58～60 min | 独立审计、汇总 PASS/FAIL | 生成一页结果摘要 |

### 10.4 一小时版必须保留的测试

以下测试不能删减：

1. 两个饮用水箱触发质量约束、两个卫生用品箱触发体积约束；
2. (s=0)、(s=0.5)、(s=0.9)、(s=1) 的充电公式边界；
3. 能耗低于、等于和高于返航余量上限的三个样例；
4. 一架无人机、一组电池、两架次的充电冲突样例；
5. 中间地形遮挡导致的通信中断；
6. 中继接入链路和回传链路时间不重叠的样例；
7. 同一架次涉及两个服务区但被分到不同任务组的非法分区；
8. 至少四个约束删除突变，并确认独立审计器能抓到违反。

### 10.5 一小时版主动删除的内容

以下内容不放入首轮一小时验证：

- 完整 15 个服务区、80 个货箱的精确枚举；
- 全部路线和全部 DEM 区域的通信可视化；
- 多随机种子、大规模参数扫描和蒙特卡洛实验；
- 所有可能的约束突变组合；
- 完整论文图表、资源敏感性分析和正式结果文件；
- 不影响硬约束判断的目标权重细调。

这些内容应在一小时核心版通过后再做。

### 10.6 一小时版通过条件

一小时内只对“小实例验证”作结论。通过条件为：

- 所有必保留测试通过；
- 小实例独立审计无硬约束违反；
- 小实例目标值与独立重算一致；
- 至少一个小实例与精确枚举最优值一致；
- 所有通信反例被拒绝；
- 第4问没有改变第3问固定方案；
- 约束突变能够被独立审计器识别。

如果时间到达 60 分钟仍有测试未完成，应报告为“验证未完成”，不能把未运行项目默认为通过。

## 11. 当前项目状态

已读取并核验 D 题题面及基础数据文件，关键资源规模和参数已确认。当前项目中仍未发现 D 题正式求解器，因此本轮建立的是不依赖正式求解器的独立审计器和小规模精确枚举器；它不把手工构造的可行方案冒充正式优化结果。能耗部分另行采用明确记录的补充物理口径完成闭环，边界见第 12 节和 Gate 结论文件。

## 12. 本轮实际执行结果

已实现 `src/d_validation/` 独立验证器，并使用真实 Excel 输入构造了 S001、S002、S015 的 8 货箱小实例。验证器包含载荷逐航段递减审计、质量/体积边界、充电与 SOC、安全返航余量、无人机/电池/中继能源组件区间冲突、连续通信与 DEM 遮挡、中继共同时间窗、问题四分区继承、固定资源编号统计、区间着色统计、资源缺口和约束突变测试。

随后按照能耗闭环方案补充运行 `src/d_verify/` 套件，结果如下：能耗物理专项 `9/9 PASS`，能耗闭环 `62/62 PASS`，小规模总套件 `61/61 PASS`。因此，在已明确记录的补充物理口径下，图片方案的五个 Gate 均通过，可将验证状态更新为 `D_FEASIBILITY = PASS`。

这里的 PASS 只表示“能耗计算、SOC/返航余量、正常样本、失败反例和 checker 已形成可复核闭环”，不表示已经完成完整 15 个服务区、80 个货箱的全局优化。题面没有把 `E_hor`、`E_up` 完全展开为唯一计算式，当前采用的补充口径为：

```text
E_hor = d / L_g(q) * E_use
E_up  = (m_empty + q) * g * h_plus / eta_up
```

如果后续得到官方补充公式或改用其他物理口径，必须重新运行能耗专项验证。

最终运行结果：

- 核心测试：`10/10 PASS`；
- 约束突变：`6/6 PASS`；
- 非能耗硬约束：`PASS_NON_ENERGY`；
- 能耗物理闭环：`PASS`（在明确记录的补充口径下）；
- 最终运行耗时：约 `0.917 s`，满足一小时边界；
- 有限候选精确枚举：176 个候选、98 个完整方案，独立基准目标 `[2, 1000.0, 2.2]`；当前手工可行方案独立重算目标为 `[3, 1000.0, 3.1]`。该差异说明“可行”不能直接等同于“最优”，但由于项目中没有正式求解器，不能据此声称求解器错误。

结果文件位于 `results/d_validation/`：

- `core_validation_report.json`
- `core_validation_report.md`
- `constraint_mutation_report.json`
- `audit_summary.csv`
