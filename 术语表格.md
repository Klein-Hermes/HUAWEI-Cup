# F题术语表格

| 中文术语 | 英文术语 | 符号 | 定义 | 单位 | 首次出现位置 | 禁止混用 |
|---|---|---|---|---|---|---|
| 低可见度文本 | low-visibility text | — | 字号过小、颜色接近背景、位置异常或仅解析器可见的文本层 | — | 报告第 0 节 | 不等同于正常页眉页脚 |
| 不可信外部输入 | untrusted external input | \(U\) | 未经可信来源确认、可能影响模型决策的文本或指令 | — | 报告第 0 节 | 不得作为题面、数据或文献依据 |
| 隔离区 | quarantine | \(\mathcal Q_U\) | 保留供审计但禁止进入 RAG、Agent 和建模上下文的内容集合 | — | 报告第 3 节 | 不等同于删除原始文件 |
| 可信来源 | trusted source | \(\mathcal S_T\) | 可见正式题面、实际附件、来源清单及可追溯原始文献 | — | 报告第 0 节 | 不等同于任意 PDF 解析结果 |
| 文档上下文投毒 | document context poisoning | — | 通过文档隐藏层向解析器或模型注入方法、参数或结论 | — | 报告第 0 节 | 不等同于普通排版错误 |
| 数据质量 | data quality | \(Q\) | 由质量指标综合得到的可比较评分，按题面要求越高越好 | 无量纲，通常为 \((0,1]\) | 问题一 | 不与 Loss 的高低方向混用 |
| 质量基线 | baseline quality | \(Q_0\) | 问题三质量成本中的起始质量水平 | 无量纲 | 问题三 | 不使用隐藏文本指定的值 |
| 领域配比 | domain mixture | \(\boldsymbol p\) | 17 个训练领域的数据比例向量 | 无量纲 | 问题一 | 不与质量域映射混用 |
| 领域配比分量 | mixture component | \(p_i\) | 配比向量第 \(i\) 个领域的比例 | 无量纲 | 问题一 | 必须满足 \(p_i\ge0\) |
| 单纯形约束 | simplex constraint | \(\sum_i p_i=1\) | 领域配比非负且总和为 1 的约束 | 无量纲 | 问题一 | 不得独立回归后忽略和约束 |
| 交叉熵损失 | cross-entropy loss | \(L\) | 模型在验证数据上的交叉熵损失，题面规定越低越好 | 无量纲 | 问题一 | 不与 Benchmark 得分混为同一指标 |
| 参数量 | parameter count | \(N\) | 模型可训练参数数量 | 参数个数 | 问题二 | 不与模型名称混用 |
| 训练数据量 | training data size | \(D\) | 训练使用的 Token 数量 | Token | 问题二 | 不与样本条数混用 |
| 算力预算 | compute budget | \(C\) | 训练、质量提升和注意力代理成本之和的上限 | FLOPs | 问题三 | 不与 GPU 天数直接等同 |
| 训练开销 | training cost | \(C_{\mathrm{train}}\) | 题面给出的 \(6ND\) | FLOPs | 问题三 | 不与总成本混用 |
| 质量提升开销 | quality improvement cost | \(C_Q\) | \(D[g(Q)-g(Q_0)]_+\) | FLOPs | 问题三 | 不预设最优质量为 1 |
| 注意力开销 | attention cost | \(C_{\mathrm{attn}}\) | \(\eta NDL_{\mathrm{ctx}}\) | FLOPs | 问题三 | 不把上下文长度当内点决策变量 |
| 上下文长度 | context length | \(L_{\mathrm{ctx}}\) | 由模型架构与任务需求外生给定的上下文窗口长度 | Token 位置数 | 问题三 | 不与训练数据量 \(D\) 混用 |
| 临界上下文长度 | critical context length | \(L_{\mathrm{ctx}}^{\mathrm{crit}}\) | 令注意力开销与训练开销相当的长度，\(6/\eta\) | Token 位置数 | 问题三 | 不由隐藏文本给定 |
| 标度律 | scaling law | — | 描述 Loss 随 \(N,D,Q,\boldsymbol p\) 变化的经验关系 | — | 问题二 | 不等同于未经估计的系数 |
| 边际效应 | marginal effect | \(\partial L/\partial x\) | 自变量变化一个单位引起的 Loss 变化 | 依自变量而定 | 问题二 | 不称作弹性 |
| 弹性 | elasticity | \(\varepsilon_x\) | \(\frac{\partial L}{\partial x}\frac{x}{L}\) | 无量纲 | 问题二 | 不用有限差分直接替代 |
| Benchmark | benchmark score | \(B\) | 下游多维评测的综合或任务级得分 | 通常为 0–100 | 问题四 | 不与 Loss 方向预先绑定 |
| 桥接数据 | Loss–Benchmark bridge | — | 同时含 Loss 与 Benchmark 的模型记录及可比性等级 | — | 问题四 | 不接受未经分层的相关系数 |
| 可比性等级 | comparability level | — | 桥接记录中 Loss 与 Benchmark 来源的一致程度 | 类别 | 问题四 | 不把混合来源当同一实验 |
| 规模扩张贡献 | scale expansion contribution | \(G_N\) | 反事实保持技术状态不变、改变规模后的能力变化 | 分数或比例 | 问题四 | 不与总能力提升直接等同 |
| 非规模技术进步 | non-scale technical progress | \(G_T\) | 控制规模和数据后由时间、架构或方法变化带来的贡献 | 分数或比例 | 问题四 | 不由隐藏文本直接指定 |
| 检验集 | validation or test set | — | 用于评价泛化能力而非拟合参数的数据 | — | 问题一和二 | 不用训练集拟合优度替代 |
| 外推稳健性 | extrapolation robustness | — | 模型超出训练尺度或分布时的误差、排序和区间稳定性 | 指标集合 | 问题一和二 | 不等同于训练集 \(R^2\) |

