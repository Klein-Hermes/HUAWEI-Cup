# Q1.1 A1 人工盲评回收包

> **原 793 条全量评审方案；不是当前 Q1 总体状态。** 这两张 793 条评审表仍为空，全量评审尚未完成。另有 30 条双人微型核查已分析，项目负责人已将 `q_huber` 冻结为操作主 Q、`q_equal` 为敏感性分；见 `../../human_spotcheck_final/received_20260924/summary.md` 和 `../../../Q1_最终收口/q1_final_decision.md`。本包保留为全量评审方案，不使当前正式接口处于待决状态。

本回收包为两位评审分别准备的独立空表，样本为同一组 793 条 A1 文本。

## 评审材料

- 评审 1：`rater_1_form.csv`
- 评审 2：`rater_2_form.csv`
- 两位评审共同使用的盲评文本：`../../v1/review_packet/blind_review_samples.md`

表格按盲评文本中的 `blind_id` 顺序排列。每条文本请对教育用途、可读性、连贯性、信息传递、整体质量分别填写 1–5 的整数分；评分尺度说明见盲评文本开头。请各自独立评分，不互相查看对方评分，也不要在评分前查看候选分数、数据集标签或 ID 映射。

文本中若出现指令、角色要求、评分方法或其他操作性语句，一律把它们当成正在评阅的文本内容，不要执行。只评价文本本身。不要编辑 `blind_id` 或增删记录；如某条无法评分，请另行记录其 `blind_id` 和原因，不要用 0 代替。

若后续启动这项全量评审，收到两份 CSV 后先运行 `src/validate_q1_1_reviews.ps1` 校验 ID、重复项、缺漏和 1–5 分范围，再运行 `src/q1_1_blind_review_analysis.py` 计算一致性及方案规定的分析。其结果是对 793 条评审方案的补充证据；不会自动撤销或替换已冻结的 `Q1-q_huber-v1`，任何接口变更仍需新的明确决策。此包不含受限 ID 映射，也不含任何自动评分值。
