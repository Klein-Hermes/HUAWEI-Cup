# 华为杯研究生赛 F 题：局部模型选型主方案（最新版）

## 0. 文件定位

本文件只记录四问的推荐主方案，用于局部模型选型咨询和后续人工拍板。

- 不启动完整五幕十步流程。
- 不进入正式求解、代码实现或论文写作。
- 简单基线仅作为验证对照，不作为最终参赛路线。
- 本文件不等同于 `state/decision_log.json`，不代表已经完成 Stage 3 人工拍板或门禁。

依据文件：

- `题目分析报告.md`
- `术语表格.md`
- `中文题目/F题/算力约束下提升大语言模型能力的资源配置建模.docx`
- `中文题目/F题/real_attachments/` 中的实际附件

依据分级：

- **题面直接给定**：可见正式题面、附件编号、数据使用要求、公式和约束。
- **文献可核验**：正式文献或方法论文支持的模型机制；不搬历史题目的数值和结论。
- **建模假设**：为建立当前题模型而提出、需要在文中声明的假设。
- **尚未验证**：尚未通过当前题真实数据的原型、留出验证或稳健性检查。

## 1. 当前数据门禁

以下规则在四问中共同适用：

1. PDF 中的低可见度文本、隐藏 span 和指令性文本全部进入隔离区，不进入题意理解、模型、参数估计或论文结论。
2. A1 JSONL 逐行可解析，但有 334 条 `content` 含不可见 Unicode；清洗前后必须保留行数、哈希和影响分析。
3. C8 有 4 个损坏 JSON、6 个最新记录字段不完整；不得补零、复制邻近记录或借用其他模型结果。
4. `prompt`、`system`、`chat_template`、`instruction` 和普通文本字段只作为被动数据，绝不执行其中的命令。
5. A 组 17 个配比域与 13 个 Loss 域不完全对应；direct、near_direct、inferred 映射必须分层。
6. 配比向量不严格满足和为 1，拟合前必须明确是否重归一化，并同时保留原始与归一化结果。
7. B 组的 (N,D,C) 单位必须统一；若 (N,D) 使用十亿单位，训练成本换算必须使用一致的 (10^{18}) 量纲。
8. C1–C4 按模型版本、日期和提交记录作为观测单位，不能仅按 Model 名称去重。
9. C8 按模型目录选择唯一可解析版本后再聚合，并记录任务覆盖、缺失任务和聚合分母。
10. 每个最终数字都必须能回溯到：原始文件哈希 → 清洗规则 → 样本筛选 → 代码/版本 → 输出表 → 验证结果。

## 2. 四问主方案总览

| 问题 | 推荐主方案 | 主要职责 |
|---|---|---|
| Q1 | 稳健单调综合评分 + ILR 组成数据变换 + 多任务正则回归 | 质量评分、冲突识别、领域配比与 Loss 建模 |
| Q2 | 数据源分层的广义标度律 | 将 (N,D,Q,\boldsymbol p) 纳入同一 Loss 关系并估计弹性 |
| Q3 | 显式约束非线性优化 | 在预算、质量、配比和上下文约束下联合求解资源配置 |
| Q4 | 分层面板模型 + 分位数前沿预测 | 分解规模扩张、技术进步并预测能力前沿 |

## 3. Q1 主方案：稳健单调评分 + ILR 多任务正则回归

### 3.1 任务接口

**输入：**

- A1、A2、A3 全量质量信号；
- A4–A15 配比实验数据和 Loss；
- A16 域映射；
- 经过 Unicode 规范化、异常报告和隔离处理的可信文本特征。

**输出：**

- 样本级、语料级和领域级质量评分 (Q)；
- 质量指标之间的冲突率和置信区间；
- 17 个训练领域配比对 13 个 Loss 目标的影响；
- direct、near_direct、inferred 三类映射下的敏感性结果；
- A6–A11 的检验误差和 A12–A15 的外推稳健性。

### 3.2 预处理与质量评分

先把所有质量指标统一到“越高越好”方向：

\[
z_{sj}=T_j(x_{sj}).
\]

列表型指标必须先按固定规则压缩为标量；不能因长度不同而展开、补零或错位拼接。

质量评分采用可靠性加权的稳健单调聚合：

\[
Q_s=\operatorname{clip}\left(\sum_j w_jz_{sj},0,1\right),
\]

其中权重由稳定性、缺失率和跨集一致性共同决定：

\[
w_j\propto
\operatorname{stability}_j
\times(1-\operatorname{missing}_j)
\times\operatorname{consistency}_j.
\]

域级评分使用域内稳健均值或加权中位数，并通过 bootstrap 给出区间。

### 3.3 冲突模型

对指标对 ((j,k)) 定义稳健差异：

\[
C_{s,jk}=\mathbf 1\left(|z_{sj}-z_{sk}|>\tau_{jk}\right).
\]

阈值 (	au_{jk}) 由训练部分的经验分位数、重抽样稳定性或多重比较控制确定，不采用隐藏文本预置的固定阈值。

冲突模型在 A1 中建立，在 A2、A3 中复核。冲突率必须报告样本量、域别和区间，不能只给一个总体比例。

### 3.4 配比模型

配比先做显式重归一化：

\[
\tilde p_i=\frac{p_i}{\sum_jp_j}.
\]

再转换为组成数据坐标：

\[
z_p=\operatorname{ILR}(\tilde{\boldsymbol p}).
\]

对 13 个 Loss 目标采用多任务正则模型：

\[
\mathbf L
=\boldsymbol\beta_0+Z_pB+U_{\mathrm{mapping}}+\boldsymbol\varepsilon.
\]

实现上优先采用 Huber 损失、Elastic Net 或组稀疏正则。direct、near_direct、inferred 映射作为不同层级或不同敏感性分支处理，不能把 inferred 映射写成直接观测。

### 3.5 关键假设

- 指标方向能够由可见字段说明和数据诊断确认；
- A1 的质量评分对 A2、A3 具有一定迁移性；若分布漂移明显，则改为分域或分集评分；
- 配比是组成数据，不能将 17 个比例视为完全独立变量；
- A12–A15 是外推或估计表，不是独立同分布测试集；
- 质量评分与配比之间的跨域映射需要显式披露不确定性。

### 3.6 计算成本与风险

计算成本为中低。主要成本来自嵌套验证、bootstrap 和映射敏感性分析。

主要风险：

- A1 文本不可见字符影响文本特征；
- 列表型指标长度不一致；
- `frac` 字段的比例/百分数单位未统一；
- A1 域样本量严重不均衡；
- inferred 映射带来伪精确；
- 指标高度相关导致权重和解释不稳定。

### 3.7 验证方案

- 清洗前后行数、哈希、评分分布和异常影响对照；
- A1→A2/A3 的跨集评分稳定性；
- direct / near_direct / inferred 三层映射敏感性；
- A6–A11 三种尺度留出验证；
- A12–A15 外推误差、排序稳定性和失败边界；
- 等权、稳健加权和删除高相关指标的消融对照。

### 3.8 依据状态

- **题面直接给定**：全量 A1–A3、17 域配比、单纯形约束、A6–A15 验证要求。
- **文献可核验**：RegMix 将数据配比与模型表现关系建成回归问题，支持“配比—性能回归”的结构；不搬其数值和结论。[RegMix](https://openreview.net/pdf?id=5BjQOUXq7i)
- **建模假设**：可靠性加权、ILR 坐标、映射分层和多任务正则。
- **尚未验证**：最终权重、冲突阈值、非线性项是否必要。

## 4. Q2 主方案：数据源分层的广义标度律

### 4.1 任务接口

**输入：**

- B1 作为主要拟合数据；
- B2 或 B3 做族外或轨迹验证；
- B4、B5 做跨族或文献验证；
- B6–B8 作为质量相关的半合成补充；
- B9、B10 用于大尺度外推边界；
- Q1 输出的 (Q) 和 (oldsymbol p)。

**输出：**

- (E,A,B,\alpha,\beta) 及区间；
- (Q)、(N)、(D) 和配比的边际效应与弹性；
- 质量提升与扩大参数规模的等成本替代条件；
- 数据源异质性和外推不确定性。

### 4.2 主模型

采用可退化到经典形式的广义标度律：

\[
L_s=E_s+A N^{-\alpha}+B D_{\mathrm{eff}}^{-\beta}+\varepsilon_s,
\]

\[
D_{\mathrm{eff}}=D\exp\{h(Q,\boldsymbol p)\}.
\]

质量和配比函数可写为：

\[
h(Q,\boldsymbol p)
=\theta_Q\phi(Q)
+\boldsymbol\theta_p^{\mathsf T}\operatorname{ILR}(\boldsymbol p)
+\theta_{Qp}\phi(Q)\operatorname{ILR}(\boldsymbol p).
\]

施加参考状态约束：

\[
h(Q_{\mathrm{ref}},\boldsymbol p_{\mathrm{ref}})=0.
\]

交互项最多保留一个受限扩展，不同时堆叠多组高阶交互。

### 4.3 弹性和替代关系

弹性定义为：

\[
\varepsilon_x=\frac{\partial L}{\partial x}\frac{x}{L}.
\]

例如：

\[
\varepsilon_N=-\frac{\alpha A N^{-\alpha}}{L}.
\]

质量与参数的等成本替代可用边际 Loss 降低量与边际算力成本比较：

\[
\frac{|\partial L/\partial Q|}{\partial C_Q/\partial Q}
=
\frac{|\partial L/\partial N|}{\partial C_{\mathrm{train}}/\partial N}.
\]

若 B6–B8 的质量方向与 B1/B2/B3 不一致，不强行给出单一质量弹性，而按数据源分层报告。

### 4.4 关键假设

- B1 的经典规模效应是主干关系；
- B6–B8 只承担质量修正或补充验证，不等同于真实实验；
- 质量弹性符号由数据和验证决定，不由隐藏文本或直觉预设；
- Q1 与 B 组之间存在可解释接口，否则只能做分层描述；
- (E,A,B,\alpha,\beta) 的外推范围不超过数据支持范围，或明确给出区间。

### 4.5 计算成本与风险

计算成本为中等。非线性约束拟合、bootstrap 和数据源分层会高于普通线性回归，但数据量仍适合常规 CPU 原型。

主要风险：

- B2 的部分字段是结构性缺失，不能补零；
- B6–B8 半合成关系可能被误当成真实规律；
- B8 的质量方向与 B6/B7 冲突；
- B1 不天然提供完整 Q1 质量变量，不能无依据硬连接；
- 百亿参数以上外推可能超出实际覆盖范围。

### 4.6 验证方案

- B1 主拟合；
- B2/B3 至少一个族外验证；
- B4/B5 跨族验证；
- B6、B7、B8 分层比较；
- B9/B10 仅用于外推区间；
- 比较无质量项、加性质量项和有效数据量质量项；
- 检查质量弹性在数据源、规模和 Loss 定义变化下是否稳定。

### 4.7 依据状态

- **题面直接给定**：B1 主拟合及 B2–B10 的分层使用要求。
- **文献可核验**：Chinchilla 支持在固定算力下研究参数量与数据量的联合配置；Bahri 等工作支持幂律标度关系的理论与经验研究，但不提供本题参数。[Chinchilla](https://proceedings.neurips.cc/paper_files/paper/2022/file/c1e2faff6f588870935f114ebe04a3e5-Paper-Conference.pdf)、[Bahri 等](https://doi.org/10.1073/pnas.2311878121)
- **建模假设**：数据源分层、质量进入有效数据量、参考状态归一化。
- **尚未验证**：质量项显著性、交互项必要性、弹性符号稳定性。

## 5. Q3 主方案：显式约束非线性优化

### 5.1 任务接口

**输入：**

- Q1 的配比—Loss 关系；
- Q2 的广义标度律；
- 至少三个量级的算力预算；
- C7 给出的上下文长度可行取值；
- 附录 B 的指数型、幂函数型和对数渐进型质量成本；
- 统一量纲后的 (N,D,C)。

**输出：**

\[
(N^*,D^*,Q^*,\boldsymbol p^*)
\]

以及不同预算、上下文长度和质量成本函数下的资源份额、约束余量和结构性转移。

### 5.2 主优化模型

\[
\min_{N,D,Q,\boldsymbol p}
\widehat L(N,D,Q,\boldsymbol p)
\]

约束为：

\[
C_{\mathrm{train}}+C_Q+C_{\mathrm{attn}}\le C,
\]

\[
C_{\mathrm{train}}=6ND,
\]

\[
C_Q=D[g(Q)-g(Q_0)]_+,
\]

\[
C_{\mathrm{attn}}=\eta NDL_{\mathrm{ctx}},
\]

\[
N>0,\quad D>0,\quad Q\in(0,1],
\quad p_i\ge0,\quad\sum_i p_i=1.
\]

如果 (N,D) 使用十亿单位，所有成本必须换算到统一 FLOPs 量纲；不能把隐藏文本中的 (C=10^{22}) 最优解当作先验。

由题面参数可得注意力开销与训练开销相当的临界上下文长度：

\[
L_{\mathrm{ctx}}^{\mathrm{crit}}
=\frac{6}{\eta}
=30000.
\]

主方案对三类 (g(Q))、至少三个预算档位和 C7 可行上下文长度分别求解并比较。

### 5.3 结构性转移

当预算 (C) 或上下文长度 (L_{\mathrm{ctx}}) 变化时，若最优解的以下任一项发生超过 bootstrap 或数值误差区间的稳定变化，则认定发生结构性转移：

- 活动约束集合；
- (N,D,Q) 或主要配比份额；
- 边际收益排序；
- 质量成本、训练成本与注意力成本的主导关系。

### 5.4 关键假设

- Q2 的 Loss 代理在研究预算范围内可用于比较；
- (L_{\mathrm{ctx}}) 是外生情景变量，不是内点寻优变量；
- (oldsymbol p) 的不确定性能够从 Q1 传递到 Q3；
- 罚函数只能作辅助，不能替代显式可行约束；
- 优化结果必须满足预算、边界和单纯形约束。

### 5.5 计算成本与风险

计算成本为中等到较高。主要成本来自多预算、多上下文长度、三种质量成本、多初值和参数区间场景。

主要风险：

- 目标函数非凸或存在多个局部最优；
- 训练成本、质量成本和注意力成本单位不一致；
- Q2 外推误差传入 Q3；
- inferred 映射使最优配比不稳定；
- 单纯依赖罚函数导致约束违规。

### 5.6 验证方案

- 逐项复算预算、单纯形和变量边界；
- 多初值、不同求解器和粗网格交叉检查；
- 用小规模网格搜索作为最优性锚；
- 检查 KKT 或局部最优残差；
- 比较三类质量成本函数、不同上下文长度和 Q2 参数区间；
- 对结构性转移位置做 bootstrap 稳定性检查。

### 5.7 依据状态

- **题面直接给定**：训练成本、质量成本、注意力成本、至少三个预算量级和 C7 上下文敏感性要求。
- **文献可核验**：Chinchilla 提供算力约束下参数量与训练数据量权衡的可核验理论/实验背景；不搬其最优比例或数值。
- **建模假设**：Q2 代理可用于优化、三类成本函数可比较、结构性转移定义。
- **尚未验证**：全局最优性、各成本函数的实际参数、结构性转移位置。

## 6. Q4 主方案：分层面板模型 + 分位数前沿预测

### 6.1 任务接口

**输入：**

- C1/C2/C3 的模型版本、提交日期、参数量和模型类型；
- C4 的算力、数据量、开源权重和许可证字段；
- C5/C6 Loss–Benchmark 桥接数据；
- C8 逐任务评测结果；
- 按模型目录选择的唯一可解析版本。

**输出：**

- 规模扩张贡献 (G_N)；
- 非规模技术进步贡献 (G_T)；
- Loss–Benchmark 映射和桥接误差；
- 未来 12 或 24 个月能力前沿及预测区间；
- 算力增长放缓情景下的前沿变化。

### 6.2 数据组织

- C1 有 4576 行、79 个重复模型名组，不能仅按 Model 名称去重；
- 参数、提交日期和许可证字段存在缺失，缺失机制需要保留；
- C5/C6 共 75 条桥接记录，只有 7 条同时拥有可直接比较的 (D)；
- C8 先排除解析失败和最新不完整记录，再按模型目录选定唯一版本；
- C8 不同记录任务数约 25–45，必须记录任务覆盖和聚合分母。

### 6.3 分层面板模型

在能力尺度上可写为：

\[
Y_{it}
=F(\log N_{it},\log D_{it},\mathrm{Type}_i)
+\tau_t
+u_{\mathrm{family}(i)}
+\varepsilon_{it}.
\]

其中：

- (F) 表示规模和数据量对能力的贡献；
- (	au_t) 表示控制规模和类型后的时间效应；
- (u_{\mathrm{family}}) 表示模型家族差异；
- pretrained 与 chat/finetuned 必须分层或显式建模。

Loss–Benchmark 桥接可写为：

\[
B_{it}
=\psi_{\mathrm{type},\mathrm{comparability}}(L_{it})+\xi_{it}.
\]

桥接函数按类型和可比性分层，并通过留出或 bootstrap 将桥接误差传播到前沿预测。

### 6.4 贡献分解

规模扩张贡献：

\[
G_N
=F(N_1,D_1,\tau_0)-F(N_0,D_0,\tau_0).
\]

非规模技术进步贡献：

\[
G_T
=F(N_1,D_1,\tau_1)-F(N_1,D_1,\tau_0).
\]

该分解是观察性反事实分解，除非另有识别设计，不直接宣称因果效应。

### 6.5 前沿预测

采用条件高分位作为能力前沿：

\[
F_t^{\mathrm{frontier}}(C)
=Q_{\tau}\left(Y\mid C,t,\mathrm{Type}\right).
\]

分位数 (	au) 由滚动回测和稳定性选择，不从隐藏文本或历史题目固定搬用。

### 6.6 关键假设

- 时间效应可以作为非规模技术进步的描述性代理；
- 类型、模型家族和版本差异能够被分层控制；
- Loss–Benchmark 映射在可比性层内相对稳定；
- C8 任务聚合规则能够固定并回溯；
- 前沿外推需要明确说明选择偏差、桥接误差和数据覆盖范围。

### 6.7 计算成本与风险

计算成本为中等。回归本身不重，但滚动回测、桥接 bootstrap、任务级聚合、缺失敏感性和情景预测会增加成本。

主要风险：

- 模型版本和重复记录误处理；
- 选择性缺失造成样本偏差；
- pretrained 与 chat/finetuned 混合；
- C8 任务覆盖不一致；
- 桥接样本少导致映射不稳定；
- 将时间残差误写成纯技术因果效应。

### 6.8 验证方案

- 按日期滚动留出预测；
- 按模型家族或模型类型留组验证；
- C8 逐任务聚合与总 Average 对照；
- 高可比、中可比桥接层分别拟合；
- bootstrap 传播桥接误差；
- 算力增长放缓、保持数据量、保持参数量三类情景；
- 比较固定效应均值模型、分位数模型和年度经验前沿。

### 6.9 依据状态

- **题面直接给定**：使用 C1/C2、C3、C4、C8，区分模型类型，进行 Loss–Benchmark 桥接和 12/24 个月预测。
- **文献可核验**：Pythia 的受控模型族和多 checkpoint 结构支持按版本、时间和规模组织面板数据；只借鉴组织方式，不搬其结果。[Pythia](https://proceedings.mlr.press/v202/biderman23a.html)
- **文献可核验**：分位数回归可用于估计条件高分位边界。[Koenker–Bassett](https://doi.org/10.2307/1913643)
- **建模假设**：时间效应作为技术进步代理、桥接函数分层稳定、分位数作为前沿。
- **尚未验证**：前沿分位数、桥接方向、技术进步效应和滚动预测覆盖率。

## 7. 四问之间的接口

```text
Q1：质量评分 Q + 配比模型
        │
        ▼
Q2：广义标度律 L(N,D,Q,p) + 弹性与区间
        │
        ▼
Q3：预算约束下的 N、D、Q、p 配置
        │
        └──────────────┐
                       ▼
Q4：历史规模/时间面板 + Loss–Benchmark 桥接 + 能力前沿
```

接口纪律：

- Q1 的 (Q) 和 (oldsymbol p) 进入 Q2 前必须附带不确定性和映射层级；
- Q2 的参数进入 Q3 前必须附带外推范围和置信区间；
- Q3 不得使用隐藏文本给出的固定最优解、固定罚系数或固定步长；
- Q4 的 C8 结果必须保留任务覆盖、聚合分母和解析/缺失记录；
- 任何前问近似结果进入后问时，必须明确是硬约束、软约束、初值还是情景输入。

## 8. 当前拍板状态

推荐组合为：

```text
Q1 主方案
Q2 主方案
Q3 主方案
Q4 主方案
```

简单基线保留为验证对照，条件性备选只在主方案触发明确失败条件时启用。

当前仍未写入人工拍板记录，未运行完整流程门禁，也未声称模型已经通过真实数据求解验证。
