# Q2 补充绘图任务单——完成回执

原任务单：`C:/Users/Zhang Wanjie/Desktop/Q2补充绘图任务单.md`。原附件保持只读；本回执单独保存在 `zwj/`，避免在用户指定目录之外生成或改写文件。

## 1. 实际生成文件

输出目录：`zwj/Q2补充图表/`

### 图 1：M0 弹性剖面

- `result_q2_m0_elasticity_profile.svg`
- `result_q2_m0_elasticity_profile.pdf`
- `result_q2_m0_elasticity_profile.png`
- `result_q2_m0_elasticity_profile_grayscale.png`

### 图 2：附件 A p-only 跨尺度表现

- `result_q2_A_p_only_cross_scale.svg`
- `result_q2_A_p_only_cross_scale.pdf`
- `result_q2_A_p_only_cross_scale.png`
- `result_q2_A_p_only_cross_scale_grayscale.png`

### 图 3：B1–B5 p 门禁状态矩阵

- `result_q2_p_gate_audit.svg`
- `result_q2_p_gate_audit.pdf`
- `result_q2_p_gate_audit.png`
- `result_q2_p_gate_audit_grayscale.png`

### 复现与说明

- `zwj/plot_q2_supplementary_figures.py`
- `zwj/Q2补充图表/图表契约.md`
- `zwj/Q2补充图表/复现清单.json`
- `zwj/Q2补充绘图任务单_完成回执.md`

PDF 是在任务单要求的 SVG/PNG/灰度预览之外追加的矢量备份，仍位于 `zwj/`。

## 2. 数据、行数、筛选与汇总口径

1. **图 1**：读取 `q2_m0_marginal_effects_by_checkpoint.csv` 全部 1,176 行；按 8 个 `B12_model_size` 分组，每组 147 点；按 D 升序；置信区间直接读取 lower/upper 字段，不重新抽样或拟合。
2. **图 2**：读取 `holdout_metrics.csv` 156 行；筛选 `simplex_ridge + closed + 三个 test split` 后 39 行；每个 split 13 个目标；宏平均为 13 个目标已有 RMSE/R² 的算术平均，不拼接样本重算。
3. **图 3**：读取 p 审计 JSON、124 行轨迹映射 CSV、验证可比性 JSON 与 Loss 协议矩阵；B1–B5 记录数分别为 1,176、1,029、4,000、57、44；已核验并联接到 Loss 的 17 维 p 向量为 0；B4/B5 为 `NOT_COMPARABLE`。

## 3. 图表契约位置

`zwj/Q2补充图表/图表契约.md`

## 4. 导出与版面检查

- 三张彩色 PNG 和三张灰度预览均为 300 DPI。
- 三张 SVG 均含可编辑文本，无嵌入式位图。
- 程序版面检查：三张图均 PASS，未发现缺字、裁切或刻度重叠。
- 文件合规检查：`check_figure.py --min-dpi 300 --strict` 退出码 0。
- 补充目录审计：`figure_audit.py` 直接读交付目录时会把 `*_grayscale.png` 误判为需要同名 SVG 的独立逻辑图；对三张正式逻辑图的 SVG+PNG 配对文件做临时隔离复审后，结果 `ok=true`、`issues=[]`、退出码 0。临时审计目录已删除，未生成没有语义价值的灰度 SVG。
- AI 读图：彩色与灰度下信息均可辨，图例不遮挡数据，数值和表格文字清晰。

## 5. 未完成项与口径偏差

- **可选图 4 未生成**：8 个模型规模内 `Spearman(D, fitted_loss)=-1.0`，残差–D 是既有残差–拟合值图的单调横轴重参数化，不能提供新增统计或验证证据，按任务单条件跳过。
- **B2 文案纠正**：任务单写“1,029 条半合成轨迹”，权威审计 JSON 是“1,029 条记录、7 条半合成轨迹”；图中采用后者。
- **图 1 布局调整**：任务单的“2×4”“上排 N、下排 D”“覆盖 8 个规模”互相冲突。实际采用一张 4×4 图，拆成两个 2×4 区块，从而完整展示 8 个规模且保持可读。
- 未修改源 CSV、JSON、既有报告、Q2 原图或原任务单；未生成可选残差图；未拟合新的 Q2 模型。

## 复现命令

在项目根目录运行：

```powershell
& 'D:\miniconda3\envs\huawei-cup\python.exe' 'zwj/plot_q2_supplementary_figures.py'
```
