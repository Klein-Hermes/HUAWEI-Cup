# Q1 遗留问题与图表退回清单

> 写给：建模手、编程手（论文手整理，2026-09-24）
> 用途：把当前**仍未解决**的事项、卡在谁那里、需要什么才能推进，逐条列清楚。
> 已解决的不在此列——本轮已核实并写进论文的部分见文末"已收口"一节。

---

## 一、Q1 仍未解决（3 项，均需逐项核查现有交付物）

### 1.1 A2/A3 原文映射缺口 —— **未解决**

- **现状**：A2 有 **16,104 / 17,523** 行未映射、A3 有 **193,752 / 203,752** 行未映射。已恢复的只有 A2 的 1,419 行与 A3 的 10,000 行。
- **更硬的限制**：A2/A3 的附件**根本不含 `content` 字段**，因此即使补齐 ID 映射，也无法在这两个扩展集上做原文层面的效度验证。
- **卡在谁那里**：需要能拿到 OpenDataLab 原始生成版本与逐记录 ID 对照的人。
- **论文怎么写的**：已按"原文链接仅覆盖部分记录，未链接记录不纳入原文层面的核验"如实写入 5.5 节。**这一条不需要再改论文，但需要确认是否还有补救空间。**

### 1.2 30 条人工评审的三项程序偏差 —— **未整改**

| 偏差 | 具体 | 影响 |
|---|---|---|
| 回收顺序未随机化 | 两份回收表**行序相同**，且均对应 reviewer1 模板 | 无法排除顺序效应 |
| 材料为中文机译 | 评审基于机译文本，非英文原文 | 只能称"对译文的微型核查" |
| 独立评分过程未核实 | 两位评审是否独立、有无沟通，无过程证据 | 一致性系数的解释力受限 |

- **卡在谁那里**：评审过程由项目负责人组织；整改需要能说明评审组织方式的人。
- **论文怎么写的**：三项偏差已逐条写入 5.5 节，并明确"该核查仅作为有限的评分一致性与候选比较证据"。

### 1.3 7 个 `rps_*frac*` 字段的语义 —— **部分解决**

- **已解决**：单位尺度。`b78e2ef` 的全量复算（51,230 行）支持按百分数尺度存储；超过 100 的少数记录源自重叠 $n$-gram 的重复计数，属公式性质而非错误。
- **未解决**：**上游精确生成公式与行级列表聚合规则仍未追溯**。其中 `rps_doc_frac_no_alph_words` 的**名称与实测语义不符**（实测是"非 ASCII 英文字母字符占比"的补数，51,230/51,230 条匹配，MAE 2.47e-7）。
- **要推进需要**：OpenDataLab 的确切生成提交 + 行级列表折叠为文档标量的代码。**没有这两样，继续换公式重算没有可验证的停止标准。**
- **论文怎么写的**：已改写 5.2.1 与 5.5 节的表述，把"单位来源无法核实"改为"上游精确生成公式未追溯"，敏感场景改述为"语义不确定性的上界探测"（数值 0.113048 / 0.255681 / 0.729556 不变）。

---

## 二、退回队员的图表（用户已定：等他重新交付再放入）

### 2.1 两张重绘流程图 —— **不合格**

`zwj/流程图重绘_20260924/{q1-flow-score,q1-flow-conflict}.html`。纯内联 SVG、无外链、无 `<script>`，这点是干净的，但：

| 问题 | 具体 |
|---|---|
| **内容（最严重）** | score 图第 9 框仍写"**当前交付：候选评分与稳健性结果已计算／整体效度验证、最终唯一 Q 仍待人工与原文验证**"；两张图的 caption/scope 仍用内部编号"**图 4.1　F 题 Q1.1…**""范围限定为 Q1.1；Q1.2、Q1.3 与题目分析报告保持独立"。**这是项目管理语，不能进参赛论文** |
| **配色** | 12 个用色与权威 Okabe-Ito **零命中**（用的是 Flat UI）；而论文现有两张图本已合规 → 换图是**倒退** |
| **重复题注** | HTML 内嵌 `<figcaption>` 与 `<p class="scope">`，而 `paper.tex` 已有 `\caption` → 直接导出会出现**两个题注** |
| **图号错误** | 内嵌"图 4.1/4.2"是手写的且**算错了**（按 `\begin{figure}` 计数应为第 4、第 5 张）。规范明令禁止手写图号 |
| **可读性** | conflict 图 viewBox 宽 1200，按 `.92\textwidth` 缩放后正文仅 **5.74 pt**、脚注 **5.02 pt**，低于 6 pt 下限 |
| **逻辑** | conflict 图第 7 框"A1 Unicode 审计"是**悬空节点**——没有任何入箭头，只出不进 |
| **其他** | 22% 空白画布；圆角半径 6/18/22/24 混用；`<defs>` 里有 3 个声明后从未引用的 marker |

**最小修改建议**：① 删掉内嵌题注与 scope 行；② 改用 §一 的语义色十六进制；③ 补第 7 框的入箭头；④ 缩小 conflict 图 viewBox 或加大字号；⑤ 导出后裁掉空白。

> ⚠ 论文里**现有**的两张流程图同样含"当前交付…""人工评分待回填""范围限定为 Q1.1…"等项目语。它们与重绘图是同一件活，**在队员交付前会一直留在论文里**。

### 2.2 `zwj/Q2补充图表/` 三张补充图 —— **需修**

内容属实（三个 Okabe-Ito 正色命中、无 colormap、无文字压数据；跨尺度图的六个宏平均已独立核对到 `Q1.3/v1/q1_3_run_report.md`）。需修：

| 问题 | 具体 |
|---|---|
| **图元与图例不符（真问题）** | 弹性图的图例项"95% cluster Bootstrap 区间"用**灰色色块**标注，但实际色带画的是**蓝/橙色**；而且色带半宽只有 5e-6–7e-6（约 y 轴跨度的 **0.005%**），**根本看不见**。建议删掉色带与对应图例项，或加一个放大插图 |
| 配色偏差 | 灰色 `#6B7280` → 权威 `#8C8C8C`；警示红 `#8C2D21` → `#B33A3A` |
| **本机无法复现** | `plot_q2_supplementary_figures.py` 第 89 行用 `["Microsoft YaHei","SimHei",…]` **覆盖了** `setup_style()` 选出的 CJK 字体，而本机两者皆无 → 在本机复跑**中文会全变成豆腐块**。已交付的 PNG/PDF 来自 Windows 机，本身没问题，但复现链在本机是断的 |
| DPI | 论文位图统一 600 dpi，这三张是 300 dpi |

---

## 三、问题二、问题三：本轮写到哪、边界在哪

两章已按六节结构写实（`paper.tex` 第 6、7 章）。**下列边界是交付物本身的性质，不是写法问题**，论文中已逐处标注：

**问题二**
- 质量方向（B6/B7）是**半合成**数据，题面明文要求不得表述为直接实验观测 → 已标注
- `p` 的独立效应**未识别**（B1–B5 中可逐行连接 Loss 的 17 维配比向量为 0），M1 未估计 → 写成识别性分析
- B4/B5 相对 B1 的损失量尺**全部不可比**（绝对可比计数 0）→ 因此不报告合并的外部验证指标
- 弹性区间只有 **8 个轨迹簇**，只作稳定性提示

**问题三**
- `C_Q ≡ 0`（Q 固定在基线），故三种 `g(Q)` 在**主支中不影响选择**——这是结果不是遗漏
- `p` 未作为决策变量（因问题二识别门禁失败）
- **未作结构性转移判定**：判据已按可证伪要求预先定义，但正式判定所需的活动约束可变性依赖问题二尚未识别的配比与质量响应 → 只报告描述性变化
- `10^{24}` 档 1176 个网格点全部可行、最优解落在网格上界，利用率仅 2.300%–11.560% → 受 B1 支持域限制，不能外推

---

## 四、已收口（本轮完成，不需再动）

| 事项 | 状态 |
|---|---|
| Q1 操作主分 `q_equal` → `q_huber` | 已确认；`8c8ef9d` 已删除冻结 `q_equal` 的历史快照，矛盾消除 |
| 30 条人工评审结果 | κ=0.765146、Spearman=0.805995、两候选 Spearman 均 0.566692、差区间跨零 → 已写入论文 |
| 7 个 frac 字段的单位尺度 | 已由 `b78e2ef` 复算证明为百分数存储 → 论文表述已改 |
| Q2 交付物 | `综合收口/v1`、`完整回答补充/v1`、两张图目录均已到位，无缺失 |
| Q3 交付物 | 17 个结果文件 + 5 个求解脚本均已到位，无缺失 |

> 附带一条非阻塞提示：`.gitignore` 写着 `results/*` 只豁免 `!results/q1_final/`，
> 而 `results/q2_closeout/v1/` 是靠强制添加进来的。它现在被跟踪，但**将来对该目录的
> 改动不会被自动跟踪**，建议把豁免规则补上。
