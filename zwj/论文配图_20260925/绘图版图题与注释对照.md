# 绘图版图题与注释对照

本文件记录“绘图版”各图实际采用的整图题目与图内说明。论文版不加整图题目和长说明；正式论文题注可参考同文件夹的 `论文版图注建议.md`。`q2-m0-elasticity` 的论文版已另行导出，保留坐标轴、面板标记和图例。

## Q1 核心图

| 文件名（绘图版/核心图表） | 图题 | 图内注释 |
|---|---|---|
| `q1-indicator-dist` | 22 项质量指标的方向统一分布 | 箱体为四分位范围，须线覆盖第 5–95 百分位；红色名称标出单位来源待核字段。 |
| `q1-weight-calibration` | 质量指标权重的构成与最终分配 | 左图对比完整度与排序稳定性，右图给出合成权重；黄色带标示权重取值范围。 |
| `q1-domain-scores` | 候选评分的域级结果及估计区间 | 点和横线表示域级评分与 95% 区间；虚线分隔 A1 抽样集和 A2/A3 扩展集。 |
| `q1-domain-transfer` | 扩展集相对 A1 的域内评分偏移 | 横线为估计区间；区间跨零时没有检出可辨别的评分偏移。 |
| `q1-rank-robustness` | 预处理场景下的域排名稳健性 | 热图为相对主口径的排名位移；正值表示名次下降，ρ表示七域排名相关。 |
| `q1-conflict-dist` | A1 七个来源域的冲突度分布 | 虚线标出各域第 95 百分位高冲突阈值，灰色实线为域内中位数。 |
| `q1-pair-conflict` | A1 中冲突最强的 20 组质量指标配对 | 按七域宏平均排序；橙色标出包含 DSIR 与一元熵的配对。 |
| `q1-conflict-external` | 冲突度指标在扩展集上的外部复核 | 点和区间表示差异估计及 95% 区间；区间跨零时未检出差异，左右面板分别为冲突度和高冲突率。 |
| `q1-model-gain` | 主模型相对训练均值基线的误差改善 | 左图比较嵌套交叉验证 RMSE，右图给出单纯形岭回归的相对改善幅度。 |
| `q1-conflict-effect` | 评分冲突度与两种评分结果分歧的关系 | 左图按样本密度着色并叠加分箱中位数；虚线为 q95 阈值，右图比较高冲突与其他样本。 |
| `q1-substitution-volcano` | 配比转移效应的方向确定性 | 灰点表示方向确定性不足 95%；蓝、橙点区分负向与正向效应，虚线标出零效应和方向无偏。 |
| `q1-share-loss-corr` | 训练配比与验证损失的相关系数结构 | 色阶为 Pearson r；红色行表示没有对应的损失目标列。 |
| `q1-model-selection` | 配比模型族的分组交叉验证误差比较 | 越靠左表示误差越低；蓝色标记为选定的单纯形岭回归，点线表示距训练均值基线的差距。 |
| `q1-holdout-1m` | 百万级同尺度留出集的预测诊断 | 左图接近 y=x 表示预测与观测一致；右图展示残差分布，并标出偏倚与标准差。 |
| `q1-cross-scale` | 测试规模对预测误差与秩相关的影响 | RMSE 与 Spearman 量纲不同，请分别读取左右轴；数值标签为各测试规模的宏平均结果。 |
| `q1-substitution` | 配比替代效应的系数结构 | 单元格为替代关联系数 βij；红色表示正向关联，蓝色表示负向关联。 |
| `q1-flow-conflict` | 评分冲突度分析与处理流程 | 流程涵盖冲突指标、A1 域内验证、扩展集复核和 Huber/等权评分处理。 |
| `q1-flow-score` | A1–A3 指标整理与质量评分流程 | 统一指标层级、ID、单位和异常标记后，汇总域内指标并形成评分。 |

## Q2 核心图

| 文件名（绘图版/核心图表） | 图题 | 图内注释 |
|---|---|---|
| `q2-m0-marginal` | 经典 M0 模型的参数量与数据量边际效应 | 左图随参数规模变化，右图随训练数据量变化；区间表示边际效应估计的不确定范围。 |
| `q2-m0-elasticity` | M0 弹性估计与 95% 区间放大图 | 上排为完整弹性尺度，下排以自身中位数为零点放大 95% 区间；灰色带仅表示轨迹内 p10–p90 跨度。两版共用换行后的图例布局；a、b 面板的区间宽度说明只在绘图版中置于各自图框上方、分行显示。论文版沿用绘图版图面，只去除整图题目和说明，并利用顶部空白放大图面。 |
| `q2-b1-residual` | B1 验证损失拟合的残差诊断 | 虚线为零残差基准；右上角给出残差偏倚与标准差。 |
| `q2-b1-support` | B1 拟合所用的参数量与数据量支撑范围 | 每个方块对应一个训练检查点，颜色表示观测验证损失；8 个参数规模共享同一数据量网格。 |
| `q2-q-sign` | 固定 N–D 匹配单元中的 Loss–Q 斜率符号 | 条形表示负斜率与正斜率占比；条形两侧数字分别为对应的匹配单元数量。 |

## Q2 补充图

| 文件名（绘图版/Q2补充图表） | 图题 | 图内注释 |
|---|---|---|
| `result_q2_m0_elasticity_profile` | 冻结经典 M0 中 N、D 弹性随训练进度变化 | 区间来自 8 条完整 Pythia 轨迹的 cluster Bootstrap；独立轨迹较少，仅作稳定性提示。主轴区间全宽至多 6.0×10⁻⁵；嵌框显示相对估计值的原始上下界，纵轴单位为 10⁻⁵。弹性是冻结 M0 的模型蕴含量，表示 N、D 效应，不是 p/Q 效应或因果效应。 |
| `result_q2_A_p_only_cross_scale` | 附件 A 的 p-only 配比模型：同尺度与跨尺度评估 | 点为领域目标，不是独立训练重复；菱形为 13 个目标指标的算术平均，无置信区间或误差棒。这是已有评估切分而非新增盲测；跨尺度表现仅说明迁移限制，不证明 B 侧 p 效应。 |
| `result_q2_p_gate_audit` | B1–B5 配比数据与验证边界状态矩阵 | 红色提示：当前没有已核验并逐行连接到 Loss 的 17 维 p 向量，正式 B 侧 p-only 拟合尚不能启动。“0”表示当前审计材料中没有已核验且连接到 Loss 的 17 维配比，不表示真实训练中的配比效应为零。B2 半合成、B3 插值、B4/B5 Loss 口径不可比是不同限制，不能合并为同一状态；颜色仅作辅助。数据来源：p 配比可行性审计、轨迹映射、验证可比性与 Loss 协议矩阵（均为已有审计结果）。 |

## Q3 核心图

| 文件名（绘图版/核心图表） | 图题 | 图内注释 |
|---|---|---|
| `q3-c7-lengths` | C7 模型最大上下文长度分布 | 柱上数字为模型数；虚线为由成本公式得到的临界长度 30,000 token。 |
| `q3-frontier` | 算力预算利用率与注意力成本份额 | 左图显示预算利用率，右图显示注意力开销份额；百分比标签便于比较不同上下文长度。 |
| `q3-cost-curve` | 不同质量成本函数下的增量算力曲线 | 纵轴为对数；水平虚线为算力预算，右侧标注预算等级，竖线标出质量基线 Q0。 |
| `q3-conditional` | 质量成本形式对首次 N/D 重分配阈值的影响 | 每个面板对应一种成本函数，颜色区分算力预算；阈值越低表示越早触发资源重分配。 |

## Q3 补充图

| 文件名（绘图版/Q3补充图表） | 图题 | 图内注释 |
|---|---|---|
| `result_q3_p1_budget_nd_frontier` | 固定 Q=Q0 的预算—N/D 参考配置（15 个离散情景） | 固定 q_huber 基线、C_Q=0；仅为 B1 实测网格和 M0 内的参考配置，不是完整 Q3 联合最优。最高预算点受 B1 支持网格上界约束；线段不表示连续预算上的估计。 |
| `result_q3_p2_bootstrap_selection_stability` | 轨迹级 cluster Bootstrap 的配置选择稳定性（1,000 次） | N/D 的 95% 区间在全部 15 个情景中退化为单点（宽度为 0），图中未人为放大；Loss 区间按原值绘制。Bootstrap 仅基于 8 条 B1 轨迹，表示既定 M0 形式与支持网格内的参数重抽样稳定性，不覆盖模型形式、外部迁移或 Q/p 响应。 |
| `result_q3_p3_resource_shift_dashboard` | 离散预算与上下文情景下的资源配置、成本构成和 Loss | 22 对相邻变化仅作描述性比较，不使用 Bootstrap/KKT 判据，也不代表正式结构转移。固定 Q=Q0 时质量成本份额为 0；最高预算情景受 B1 支持网格上界约束。 |
| `result_q3_p4_q_baseline_sensitivity` | q_huber 与 q_equal 基线的固定-Q M0 敏感性对照 | 15 个相同预算×上下文情景中，N、D 和 M0 Loss 的最大绝对差均为 0。两支均固定在各自 Q0，故 C_Q=0；当前 M0 不含 Q→Loss 项。该图不是 Q 收益估计或 Q/p 联合优化。 |
