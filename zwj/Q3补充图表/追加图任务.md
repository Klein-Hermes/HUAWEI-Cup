# Q3 后续补图任务单：编程手

**日期：** 2026-09-25  
**任务状态：** 已选定两项；图件已在 Q3 结果目录生成，需归档副本到 `zwj/Q3补充图表/追加图_20260925/`。  
**归档原则：** 复制归档，不移动、不覆盖现有文件；`zwj/Q3补充图表/` 中既有 P1–P4 图件及其清单保持原样。

## 任务目标

结合 Q3 现有图件，追加能补足解释链的两个图件主题：

1. **S4：质量增量成本曲线。** 展示题面三种质量成本函数在 B1 观测 D 支持域上的成本规模。
2. **Q3 分支流程图：**区分已计算的固定 Q₀/M0 分支、依赖未知 βQ 的条件 Q-only 分支，以及尚未识别的完整 Q/p 联合优化门槛。

T4 已有 βQ 临界值热图，不重复绘制该主题。

## S4：质量增量成本曲线

- **数据源：** `Q3/04_结果/Q_cost_increment_envelope.csv`；解释口径参考 `Q3/04_结果/Q3_cost_sensitivities_report.md`。
- **数据覆盖：**36 行，三种 `g(Q)`（`exponential`、`power4`、`logarithmic`），质量目标从 Q₀ 到 1.00；D 参考为 B1 观测支持的最小值、中位值和最大值。
- **图型：**按 D 最小/中位/最大值分成三个面板，比较三种函数对应的增量 FLOPs；纵轴为对数尺度，各面板使用清楚标示的 D 值。
- **零点处理：**冻结基线 Q₀=0.4984781048 时，C_Q=0。对数轴不画零点，不使用伪计数；在图注中说明零基线，并清楚标出绘制的正成本质量范围。
- **解释边界：**这只是题面代理公式计算出的成本，不代表质量提升带来的 Loss 收益，不据此选择最优 Q，也不作新优化或新拟合结论。

## Q3 分支流程图

- **固定 Q₀ 主支路（已计算）：**Q1 `q_huber` 接口给出 Q₀；Q2 B1/M0 提供 1,176 个实测 N/D 点；C7 提供 context 档位；结合题面成本函数，得到固定 Q₀ 的预算参考前沿、既有轨迹 Bootstrap 与预算—context 描述性结果。
- **条件 Q-only 支路（条件分析）：**从固定 Q₀ 的 M0 Loss 出发，引入显式未知 βQ，βQ 不由 Q2 估计，p 不进入模型；输出只解释“若给定该响应假设，何时发生 N/D 重分配”。
- **完整 Q/p 联合优化（尚未识别）：**仍需可追溯配比 p、独立的 Q 变化，以及能够连接到对应训练配方和 Loss 的响应证据。将此标为上游数据门槛，不画成已完成求解。
- **来源核对：**`Q3/03_代码/solve_q3_m0_reference_grid.py`、`Q3/03_代码/solve_q3_conditional_q_sensitivity.py`、`Q3/04_结果/M0_ND_reference_frontier_report.md`、`Q3/04_结果/Q3_conditional_Q_optimization_report.md`。

## 归档和交付

目标位置：`zwj/Q3补充图表/追加图_20260925/`。按下列结构归档：

```text
追加图_20260925/
├── S4_质量增量成本曲线/
│   ├── result_q3_supp_quality_incremental_cost.png
│   ├── result_q3_supp_quality_incremental_cost_grayscale.png
│   ├── result_q3_supp_quality_incremental_cost.svg
│   ├── 图表契约.md
│   └── 绘图脚本或可复现脚本入口说明
├── flow_q3_model/
│   ├── flow_q3_model.png
│   ├── flow_q3_model_grayscale.png
│   ├── flow_q3_model.svg
│   ├── 图表契约.md
│   └── 绘图脚本或可复现脚本入口说明
├── README.md
└── 归档清单.json
```

现有可归档源图件和脚本位于 `Q3/04_结果/figures/supplementary_20260925/`。允许直接复制经核验的成图；脚本如依赖原目录相对路径，应在归档说明中记录原脚本的项目相对路径与复现命令，避免留下不可运行的伪入口。

## 质量要求与限制

- PNG 至少 300 DPI；提供可编辑 SVG 和同尺寸灰度预览。版面需通过脚本布局门禁，并实际检查彩色与灰度图的文字、线型、图例、裁切和重叠。
- 归档副本逐文件计算 SHA-256，在 `归档清单.json` 记录来源、目标相对路径、字节数和哈希；核对归档前后哈希一致。
- 只使用上述已存在数据、代码和报告；不重跑拟合、Bootstrap 或优化，不修改冻结包、Q3 现有图件及 zwj 原有 P1–P4。
- 若全局 `figure_audit.py --questions q3 --strict` 失败，记录实际退出码和范围原因，不得把局部图件门禁写成全局通过。

## 当前源图件核验记录

源图件已完成生成脚本运行、布局门禁和彩色/灰度目检。S4 彩色/灰度 PNG 为 2002×2034、300.99 DPI；流程图彩色/灰度 PNG 为 4237×2641、300.99 DPI；SVG 保留文本与矢量路径。Q3 全局严格图审退出码为 1，不能据此宣称 Q3 全局图审通过。编程手归档后仍需复核目标文件哈希与归档清单。
