# Q3-T5 最终报告与本轮结果包执行记录

**日期：**2026-09-25  
**状态：**本轮可识别结果范围已整合并生成日期版本冻结快照。  
**范围说明：**此状态只覆盖固定 $Q=Q_0$、冻结 M0、B1 已观测 N/D 支持域下的 T1/M3/M4、T2、T3 结果，以及 T4 条件 βQ 敏感性；不表示完整经验 Q/p 联合最优，也不表示竞赛论文提交 QA 完成。

## 执行流程与结果

| 步骤 | 执行方式 | 结果 |
|---|---|---|
| 锁定来源 | 读取 T1、M3/M4、T2、T3、T4 权威 CSV 与任务级 manifest；只读消费既有分析结果 | 完成。未重新拟合 M0、未重跑 Bootstrap、未重算 T2/T3/T4 |
| 接入 T2 | 按预算 FLOPs × 上下文 tokens 合并 T2，并核对预算可行数与 T1/M4 | 完成。17,640 条候选—情景、1,865 个 Pareto 点、10 条边际收益记录纳入汇总；三类明细继续保留专题表 |
| 生成总表 | T1 15 个场景为主键，追加 T2 可行与近优计数、Pareto 可行计数、相邻预算边际收益、T3 重选摘要、T4 条件阈值摘要 | 完成。`q3_master_table.csv` 为 15 行、69 列；T2 可行点数逐情景与 T1/M4 一致 |
| 保留专题结果 | T2/T3/T4 的细粒度结果按任务单独保存；T3 的 N/D 配置切换与成本结构切换分开 | 完成。未把 22 对转移或 166 个条件区间压缩进 15 行主表 |
| 刷新清单与快照 | 生成 figure/table/reproduction manifests、`frozen_numbers.json`、来源代码快照和 SHA-256 文件清单 | 完成。快照位于 `Q3/冻结包_20260925/`；主报告六张图的本地链接均检查 |
| 完成本轮 QA | 核对源 manifest 输入输出哈希、行列数、主键覆盖、图件与报告链接、声明范围和冻结包哈希，并做独立整包 P2 复核 | 限定范围核对与独立整包 P2 均通过；最终 114 项文件哈希、33 张表、32 个图件和 21 个报告链接通过。回执见冻结包 `QA/P2_independent_release_receipt.md`；原生 submission QA 仍 NOT RUN |

## 冻结目录

```text
Q3/冻结包_20260925/
├── README.md
├── q3_final_report.md
├── q3_master_table.csv
├── frozen_numbers.json
├── figure_manifest.csv
├── table_manifest.csv
├── reproduction_manifest.md
├── scope_decision_ledger.md
├── release_manifest.json
├── 01_fixed/
├── 02_robustness/T2/
├── 02_robustness/T3/
├── 03_conditional/
├── QA/
└── code_sources/
```

本快照用于固定本轮范围与结果。若源数据、脚本或分析结果变化，应另建日期版本并重新做哈希核对，不覆盖本快照。

## 结论与未完成事项

- T1/M3/M4、T2、T3 与 T4 的本轮结果、报告、细表、图件与复现信息已收入快照。
- 完整经验 Q/p 联合优化仍未识别：Q2 目前不能分离独立质量响应，`p` 也未进入当前目标。T4 的 βQ 只作条件 break-even 参数，不作经验估计。
- 项目尚无活动 `rigor_profile`。因此已完成的是明确标注的 Q3 范围包完整性、一致性、可复现性核对；`completeness-auditor` 的原生活动配置审计和 submission-level `quality-assurance-auditor` 不标记为通过。
- 本包不是完整竞赛论文、竞赛提交文件或完整 Q/p 联合最优；若要完成这些事项，须先取得 Q2 的 p 和独立 Q 响应识别证据，再按对应提交规则另审。

## 2026-09-25 rev2 增补：M0 函数形式敏感性与整轨迹留一

用户同意将新增分析并入 Q3 正文并发布为新版本。旧 `冻结包_20260925/` 和预算分析快照均保持不变；新的限定范围发行版位于 `Q3/冻结包_20260925_模型形式敏感性_独立复核/`。

- 正文新增 pooled 8 折整轨迹留一 RMSE、拟合启动收敛、全样本情景选点变化、逐折配置一致率与边界说明。两个受限形式对全样本加 8 折的 9 次拟合均为 16/16 初值收敛。
- 冻结 M0 全样本配置在轨迹留一中保持 120/120；去掉 Loss 下限后 RMSE 为 0.0728758 且全样本选点变化 9/15；共享指数 RMSE 为 0.0142510 且变化 1/15。结论仍限于固定 Q₀ 和当前 B1 候选支持域。
- rev2 包复用既有 15×77 汇总主表与 7 张主报告图；函数形式结果作为独立专题表收入包，不改写 M0 主表选点，也不增加重复图件。
- 限定范围静态与独立 P2 复核通过：7 份任务 manifest 的 132 项文件哈希无缺失/不符；43 张表、36 个图件文件和 27 个本地报告链接通过；候选包登记 140 项哈希/字节数全部匹配。P2 回执位于 rev2 包的 `QA/P2_independent_release_receipt.md`。
- 该 P2 只覆盖 rev2 可识别结果包、模型形式敏感性及报告引用一致性。没有活动 `rigor_profile`，故原生 submission 完整性审查仍未运行；完整经验 Q/p 联合优化仍未识别。

## 复现

从仓库根目录使用 `Q3/冻结包_20260925/reproduction_manifest.md` 给出的任务级命令和总表组装命令。上游输入文件路径与哈希保留在 T1/M3/M4/T2/T3/T4 的任务 manifest 中；完整重跑需要项目中的 Q1/Q2 上游数据。
