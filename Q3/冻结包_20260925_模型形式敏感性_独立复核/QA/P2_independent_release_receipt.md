# Q3 本轮结果包 rev2 独立 P2 复核回执

**状态：PASS。** 范围覆盖 rev2 报告、函数形式/整轨迹留一专题结果、来源清单、包内表图清单、链接和文件哈希；不代表完整经验 Q/p 联合优化或竞赛提交级终审。复核者只读检查，未修改候选包或权威输入。

- **包清单：**`release_manifest.json` 登记的 140 项文件均存在，SHA-256 与字节数全部一致。复核时的候选包总文件数为 141（含清单自身）。
- **来源清单：**7 份任务级 manifest 共核验 132 项文件哈希；缺失 0、哈希不符 0。函数形式专题的 6 张 CSV、报告、任务 manifest、两份 P1 回执、计划和代码快照均已纳入，包内副本与权威源匹配。
- **表、图和链接：**43/43 张表、36/36 个图件文件的路径与哈希均匹配；报告引用的 7 张主图编号正确，27 个本地 Markdown 链接均可解析且留在包目录内。
- **独立复算：**pooled 整轨迹留一 RMSE 为当前 M0 0.0001511798、去掉损失下限 0.0728758137、共享指数 0.0142509534；全样本选点变化为 0/15、9/15、1/15；留一选点匹配当前 M0 全样本方案为 120/120、50/120、112/120。正文、`frozen_numbers.json` 和专题 CSV 一致，变化情景逐项核对一致。
- **拟合与边界：**两个受限形式各执行 9 次拟合（全样本加 8 折），每次 16/16 个优化初值收敛。正文将该数量表述为多启动收敛，且明确整轨迹 pooled OOF、固定完整候选网格与最大支持边界的解释限制。最高预算选点为 N=11.965825B、D=299.893B；正文未外推为网格外最优。
- **范围：**冻结仍固定 Q=Q₀、排除 p，并受限于 B1 观测支持域；不宣称完整 Q/p 最优、因果效应、独立外部验证或 submission QA 通过。
- **旧快照：**既有预算分析 release 的 128 项登记文件仍全部匹配，本次没有改动旧快照。

复核未发现阻断问题。写入本回执后，使用 `assemble_q3_scoped_release.py --release-dir Q3/冻结包_20260925_模型形式敏感性_独立复核 --refresh-release-manifest` 刷新总哈希；刷新后再次核对清单登记文件。

## 结论段补充复核

首轮 P2 后，正文结论段补入模型形式/整轨迹留一发现、固定 Q₀ 参考模型定位、同源 Pythia 验证范围和完整候选网格边界。独立补充审查 PASS：该段与包内专题结果一致，工作报告与包内副本逐字相同；27 个链接仍有效，140 项登记文件哈希/字节数仍全部匹配。补充审查未改写文件；当前 `release_manifest.json` 已在该报告更新后再次刷新。
