# 图表契约：固定 Q₀ 下的预算饱和与有限网格切换

横轴为绝对预算 C（FLOPs），纵轴为 1,176 个精确观测 B1 候选的预算可行比例；每条阶梯线对应一个上下文，彩色竖虚线和圆点表示该上下文的 C_sat，灰色竖点线表示 1e19、1e22、1e24 三档正式预算，横虚线表示 100% 候选可行。由于成本可写成 ND×(6+L/5000)，相对 C_sat 的五条曲线会完全重合，因此图中使用绝对预算轴。数据来自独立审计成本网格，并按题面代理成本公式逐点复算。图中不插值、不外推，也不估计连续 KKT 转折。正式 1e19/1e22/1e24 情景的精确覆盖数字见 CSV 表。

输出为 SVG/PDF/300 DPI PNG 和灰度 PNG；使用数学建模 figure helper 做导出及版面预检。该图是当前 Q3 报告的第 7 图候选，不代表全 Q3 全局图审计通过。
