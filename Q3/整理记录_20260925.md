# Q3 文件夹整理与历史遗留审查

**日期：** 2026-09-25  
**范围：** Q3 目录结构、入口文档、版本包索引、历史状态文字、复现路径引用。未重跑模型或改动数值结果。

## 发现与处理

| 项目 | 发现 | 处理 |
|---|---|---|
| 当前冻结状态文字 | 根 README 的 T3 阶段描述和 T3 执行记录保留了“Q3 整体未冻结”；它们描述的是 rev2 发布前的阶段状态，单独阅读会与当前冻结状态冲突。 | 将这些句子标注为阶段记录，并链接到当前 rev2 入口。 |
| 当前版本链接 | 阶段总结仍把早期 `冻结包_20260925/` 作为当前交付状态入口。 | 更新为当前 rev2 冻结包；早期入口保留在历史版本说明中。 |
| 多个冻结包并列 | Q3 根目录有一个当前 rev2 包和三个更早的完整快照，合计约 120 MiB。快照内容大量重叠，但每个版本都有独立 manifest/P2 回执，且路径被清单或执行记录引用。 | 不移动、不覆盖、不去重；在根 README 添加唯一当前入口及历史版本导航，避免破坏哈希和引用。 |
| 补充图候选目录 | `补充图_候选20260925/` 名称看似临时目录，但当前补充图 manifest 仍引用其中 S1–S3 的源脚本和图件。 | 保留原路径，并说明它是图集的来源材料；最终图集入口指向 `04_结果/figures/supplementary_20260925/`。 |
| 归档目录入口 | `归档_20260925/` 包含早期图包、任务记录和隔离复核样例，但没有目录索引。 | 新增 `归档_20260925/README.md`，区分历史材料和权威结果。 |
| 生成缓存 | 归档中有一个旧 `.pyc` 字节码文件，无分析数据或复现用途。 | 删除该生成缓存；对应 Python 源文件仍保留在权威代码目录。 |
| 预算覆盖主图说明 | 根 README 把主图横轴写成相对 `C_sat`，但图表契约和绘图代码实际采用绝对 FLOPs 轴；相对轴会令五个上下文曲线重合。 | 已按图表契约修正根 README 图注，说明绝对预算轴及 `C_sat` 标记。 |
| 补充图复现引用 | 初查发现 P1–P3 绘图脚本和图件清单仍引用 `zwj` 路径。 | 将共享绘图脚本和论文版脚本纳入 `03_代码/图表源/`，改为项目内输入及 Q3 暂存输出；补充图和候选图清单改指 Q3 本地副本。原 `zwj` 文件保持原样。 |

## 当前目录使用方式

- 当前结果只从 [`冻结包_20260925_模型形式敏感性_独立复核/`](冻结包_20260925_模型形式敏感性_独立复核/README.md) 进入；整包 P2 对包内 140 项登记文件、43 张表和 36 个图件文件核对通过。
- `01_方案与设计/`、`02_数据审计/`、`03_代码/`、`04_结果/` 保持原路径，供源材料和复现使用。
- 其余三个 `冻结包_*` 目录是历史快照；它们不代表当前正文版本，但各自的哈希和复核记录继续有效。
- `_q3_fixedq_p1_smoke/`、`_figure_previews/` 等中间材料在复现清单中有记录，暂时保留，不作为最终结果或论文图件。

## 仍需注意的非目录问题

Q3 的正式范围仍是固定 $Q=Q_0$、不优化 $p$、限于 B1 实测 N/D 支持域的参考配置及条件敏感性；完整经验 Q/p 联合效应未识别。该限制已经写入冻结报告，不属于本次文件清理能解决的问题。

本次检查时 Git 对 Q3 的多个交付物显示为未跟踪文件，README 和方案报告也有未提交修改。冻结包的哈希证明其内部一致，但不代表这些文件已进入 Git 历史；后续应在确认仓库纳管策略后再统一提交。

## 本轮补充核查：`zwj` 目录异常

只读盘点显示 `zwj/` 共 9,685 个文件系统项目（4,666 个文件、5,019 个目录）。其中 4,452 个文件为 0 字节；1,542 个根层空文件最后写入于 2026-09-25 17:41，2,888 个根层目录集中写入于 17:41–17:45（其中 2,838 个在 17:41）。这些条目的名称多为无扩展名、外观像随机或编码后的字符串。Git 当前对该目录报告 4,377 个未跟踪路径和 52 个已跟踪路径删除。另有正常的 Q2/Q3 图稿、脚本和文档，因此不能把整个 `zwj/` 目录都视为垃圾。

这批空文件和随机目录更像某次异常批量导出、解包或同步产生的残留；这是根据命名、大小、时间聚集和 Git 状态作出的推断，现有文件系统与 Git 记录不足以锁定具体程序或操作。此前的 Python 异常弹窗也不能单独证明它就是来源。检查确认 Q3 内已有两套图件副本：正式补充图集 27 个文件、候选图集 24 个文件。P1–P3 绘图脚本此前仍只在 `zwj`；本轮已复制并调整到 `Q3/03_代码/图表源/`，图件清单已改指 Q3 内文件。迁移核对时，候选图集 24 项及正式 P1–P3 的 12 项均与原图 SHA-256 相同；当前清单再核对本地 27 项正式图和 24 项候选图的哈希/字节数均通过。候选替代版式的原图件导出目录没有附带生成脚本或复现清单，所以 Q3 保留了静态图件副本；正式 P1–P3 则有 Q3 内绘图源。当前可复现入口不读取 `zwj/`；其中 `zwj` 仅作为历史来源记录，图表迁移当时只读取并复制素材，没有移动或删除 `zwj/` 原件；本轮后续对零字节批量残留的清理另见下方记录。

`zwj/` 目录中 52 个已跟踪删除有 51 个位于旧 Q1 文件路径，另 1 个是图片；它们与 4,377 个未跟踪路径是两类状态。需要结合历史版本和目录所有者另行处置，不能据此直接清空整个目录。

## 本轮清理：zwj/ 批量空残留

- 已删除根层 1,542 个 0 字节、无扩展名文件，以及 2,887 个时间集中在 2026-09-25 17:40–17:50、名称不透明的目录树；这些目录树内共 2,910 个文件，均为 0 字节，另含空目录。删除前核对了路径边界、文件大小、重解析点和 Git 索引路径（匹配数为 0）。
- 保留 流程图重绘_20260924/、Q2补充图表/、Q3补充图表/、论文配图_20260925/，以及根层脚本、Q1/Q2 文档和 .gitkeep；未改动已跟踪删除状态。
- 清理后 zwj/ 仍有 214 个文件（0 个空文件）和 13 个目录；保留的有内容素材未做移动或覆盖。
- 该批残留的具体来源仍无法仅凭文件系统证据确定；时间、命名及空文件特征支持“异常批量导出/解包/同步残留”的推断。
