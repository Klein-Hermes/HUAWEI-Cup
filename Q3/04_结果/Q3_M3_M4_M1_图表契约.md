# Q3 M3/M4/M1 图表契约

**范围：** 当前固定 Q 的 B1/M0 离散参考支路。绘图使用 Python、通用中文报告样式，成图宽度 7.2 英寸以内；矢量 PDF/SVG + 300 DPI PNG，并保留灰度预览。图中 N、D 以十亿单位显示，对数坐标与 M3 凸包坐标空间一致。

| 图 | 类别 / 核心结论 | 图型与数据映射 | 解释边界 |
|---|---|---|---|
| `raw_q3_observed_nd_support` | 原始数据：B1 实测点构成完整 8×147 N/D 网格；对数空间凸包只概括几何范围。 | N/D 对数散点图；实测点与凸包边界。 | 凸包不自动证明插值可行；当前主分析只允许实测组合。 |
| `process_q3_m3_m4_mask_audit` | 过程审计：M3 对每个 N 值均保留 147 个精确实测点；M4 可行数量随预算和上下文改变。 | 左：按 N 分组的支持类别堆叠条图；右：上下文×预算的 M4 可行点数热图。 | 15 个情景、每格以 1,176 个观测候选为分母；不是概率或质量提升率。 |
| `result_q3_m1_budget_optima` | 结果：15 个预算×上下文配置均位于 M3 允许且 M4 可负担的实测支持点中。 | 五个上下文小面板；灰点为 B1 支持网格，彩色形状为三档预算的 M1 最优点。 | 最优点是固定 Q 的 M0 参考；最高预算的选择触及 N/D 上界，不向支持域外推。 |

没有绘制置信区间或显著性标记：这些图展示的是离散支持、可行性和已选配置，不是抽样估计比较。M1 的参数不确定性另见 Bootstrap 审计，不在这三张图中重复呈现。
