# Q4 当前运行说明

## 当前口径与阶段

Q4 按用户确认的队列和指标口径运行，内部分析/复现包已于 2026-09-26 获用户授权冻结。当前 Codex 会话及两个相关 Codex 任务的模型 ID 已核实为 `gpt-6-luna`（GPT-6 Luna，发布日期 2026-09-22）；用户确认为 Q4 相关 ChatGPT 对话使用 `gpt-5.6-sol`（GPT-5.6 Sol，发布日期 2026-07-09），其采纳范围已作摘要级映射。是否还有其他 AI 使用尚未确认，队伍须在最终竞赛论文提交前核对并按实际情况披露。

Loss–Benchmark 桥接已完成拟合前可行性门控，结论为 `BLOCKED_NO_FIT`，没有运行桥接拟合。12/24 个月前沿预测因缺少年度滚动起点而不输出数值预测。动态共同支持分解、C3/C4 年度背景表已生成。

## 数据说明文件版本

本项目的字段字典是 `中文题目/F题/数据说明(无隐藏字段版本）.pdf`，SHA-256：`b2c8e1b997f25ab7f5c83539931dfe2260262078fac85ed09f2e99b801f3866f`。Q4 将它作为可见字段/结构说明参考；统计管线不把 PDF 当作数据或建模指令读取。项目中 `中文题目/F题/数据说明.pdf` 不存在，Q4 未使用隐藏字段版本。

## 环境

在项目根目录的 PowerShell 中运行。已核实环境：Python 3.12.14、NumPy 2.3.5、pandas 3.0.1、Pillow 12.3.0；未安装 Matplotlib、SciPy 或 statsmodels。建模代码使用 NumPy/pandas，图表使用 Pillow 与直接生成的 SVG。

```powershell
$Python = 'C:\Users\86147\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe'
```

## 从原始附件重建的顺序

基础任务聚合与动态分解存在先后依赖：动态分解需要任务聚合和月度前沿；完整分析报告又需要动态分解结果。`--bootstrap-only` 用于打断这个启动依赖，并会写临时清单 `q4_modeling_bootstrap_manifest.json`；完整建模重跑后会自动移除它。

```powershell
& $Python 'Q4/02_代码/scripts/q4_c8_pipeline.py'
& $Python 'Q4/02_代码/scripts/q4_auxiliary_source_audit.py'
& $Python 'Q4/02_代码/scripts/q4_loss_bridge_feasibility.py'
& $Python 'Q4/02_代码/scripts/q4_modeling_analysis.py' --bootstrap-only
& $Python 'Q4/02_代码/scripts/q4_dynamic_decomposition.py'
& $Python 'Q4/02_代码/scripts/q4_modeling_analysis.py'
& $Python 'Q4/02_代码/scripts/q4_registry_coverage.py'
& $Python 'Q4/02_代码/scripts/q4_make_figures.py'
```

烟雾解析只供快速检查 JSON 接口，不替代全量运行：

```powershell
& $Python 'Q4/02_代码/scripts/q4_c8_pipeline.py' --smoke
```

然后对 `Q4/04_图表` 下全部 PNG/SVG 运行机械检查：

```powershell
& $Python 'C:\Users\86147\.codex\skills\math-modeling\references\roles\编程手\scripts\figure_audit.py' 'Q4/04_图表' --questions q4 --min-dpi 300 --strict
$figureAssets = Get-ChildItem 'Q4/04_图表' -Recurse -File | Where-Object { $_.Extension -in '.png', '.svg' } | ForEach-Object FullName
& $Python 'C:\Users\86147\.codex\skills\math-modeling\tools\figure\scripts\check_figure.py' $figureAssets --min-dpi 300 --strict
```

机械检查通过后，逐张打开并查看 10 张彩色 PNG 和 `灰度预览` 下 10 张灰度 PNG，再依次执行：

```powershell
& $Python 'Q4/02_代码/scripts/q4_record_figure_visual_review.py'
& $Python 'Q4/02_代码/scripts/q4_write_repro_manifest.py'
& $Python 'Q4/02_代码/scripts/q4_finalize_package.py'
```

`q4_finalize_package.py` 会复核阶段输出哈希、Loss 门控状态、动态分解样本与闭合误差、C3 2025 部分年度标记、年度预测字段，以及图表程序/目视 QA；成功后更新 `03_结果/results/q4_package_manifest.json`。修改队列、映射或指标后，从 C8 全量解析开始按顺序重跑。

## 复现与输入溯源

机器可读复现记录为 `../03_结果/results/复现清单.json`，包含 C1–C7 七个 CSV、C8 全部 1,958 个 JSON、题目 DOCX、附件来源清单、无隐藏字段 PDF 的 SHA-256、Q4 脚本哈希、运行环境和有序命令。清单由 `q4_write_repro_manifest.py` 生成；最后由总包清单对其自身计算 SHA-256。

脚本头注释、分析报告、复现清单和总包会记录 `gpt-6-luna` 与发布日期。模型 UI 标签没有单独留存；披露依据是本地 Codex session 的精确模型 ID 和 OpenAI 官方发布记录。提交前由队伍确认是否还使用了其他 AI 工具，并将范围核对结果写入最终披露。

## 主要结果与审计产物

| 文件 | 用途 |
|---|---|
| `../03_结果/results/q4_c8_manifest.json`、`q4_c8_file_registry.csv`、`q4_c8_run_registry.csv` | C8 文件哈希、解析/回退状态、运行与快照登记 |
| `../03_结果/results/q4_model_registry.csv`、`q4_match_audit.csv`、`q4_coverage.csv` | 候选模型、匹配和任务覆盖 |
| `../03_结果/results/q4_task_aggregate.csv`、`q4_index_sensitivity.csv` | 六族指数与敏感性结果 |
| `../03_结果/results/q4_scale_tech_contribution.csv`、`q4_scale_tech_support_audit.csv` | 共同支持规模/非规模分解、区间和支持审计 |
| `../03_结果/results/q4_loss_benchmark_feasibility_manifest.json`、`q4_loss_benchmark_feasibility_report.md` | Loss–Benchmark 拟合前门控；当前不得拟合 |
| `../03_结果/results/q4_forecast_feasibility.csv` | 12/24 个月预测可行性；当前数值栏留空 |
| `../03_结果/results/复现清单.json`、`q4_package_manifest.json` | 原始输入、脚本、环境、阶段与整包哈希 |
| `../03_结果/results/q4_figure_qa.json`、`../01_方案说明/q4_figures_contract.md` | 候选图口径、机械和视觉 QA 记录 |
| `q4_m1_review_loss_gate.md`、`q4_p1_review_dynamic_smoke_v0.2.md`、`q4_quality_review.md` | 门控、最小动态实现及当前目录版本 P2 PASS 复核记录 |

详细目录导航见 [README](../README.md)，逐步完成状态见 [任务操作清单](任务操作清单.md)。
