# Q4 修订推进清单（2026-09-26）

**当前判断：** 旧版 Q4 内部分析/复现包曾经按用户授权冻结；这不等于“Q4 研究问题全部有数值答案”，也不等于整篇 F 题论文可提交。用户现在要求继续推进，因此本目录以历史包为基线开展修订，修订版暂不冻结。

| 截图所列事项 | 现有证据状态 | 当前处理 |
|---|---|---|
| C1/C2 + C3 | 已有：C1/C2 支持模型队列与元数据；C3 的 Open LLM Leaderboard 作为独立年度背景，不跨来源强行拼成同一模型 | 沿用，论文需说清 C3 是独立背景序列 |
| C4 算力、数据量、开源权重字段 | 已有年度表，含算力/数据量/参数的有效数、中位数、P90，Confidence 分类及 Open Weights Yes/No/unknown 计数；C4 与 C1 精确交集为 0 | 沿用为年度背景，不将其伪装成 C1 个体训练成本 |
| C8 逐任务聚合 | 已有逐模型逐叶任务结果，但总报告侧重综合指数；逐任务影响诊断尚未单独呈现 | 本修订新增 BBH 24 个叶任务的逐项剔除敏感性，报告名次稳定性与任务分布 |
| 规模/非规模贡献占比 | G4 补出描述性点份额：规模 −180.57%、类型 +103.29%、月份 +177.28%、非规模合计 +280.57%；观测净变化区间跨 0，Fieller 比率集合无有限界 | 点份额可列作描述性分解，不得称为稳定识别的贡献比例或因果占比；分量区间和比率不可识别性必须同时报告 |
| C1 无条件分数趋势 | 月度目标是 C1 `Average` 原始分数构造的开放权重 P90 前沿；它不是 Loss 转换结果 | 无条件 C1 趋势仍不能由短序列外推；但这不等于条件情景无数值。Q4/07 的 C4 条件预测从数据末月 2025-03 起算 |
| 算力放缓条件预测 | 目标仍为 C1 原始分数；C4 仅锚定三条公开标注的算力情景，不估计个体能力—算力关系 | 按用户明确指示以 v0.8 条件情景补充覆盖 v0.7 留空规则，输出 12/24 月条件点值和模拟分位数带，并做 α=.73 敏感性；年度预测技能仍 `NOT_VALIDATED`、模拟带覆盖率 `UNKNOWN` |
| Loss–Benchmark 映射 | v0.6 严格门控是历史状态。Q4/08 按 High/Medium 分层对 C5 主表及 C6 扩展表分别拟合，并采用留一尺寸/留一发布者验证 | C5 High、C5 Medium 留出误差均未优于均值基线；C6 Medium 扩展表仅小幅优于基线（MAE 9.515 vs 10.057），仍未校准。当前可报告分层探索映射与误差，但不得作为跨家族可靠转换器或接入主预测 |
| 权重开放与许可证 | 201 个 C2 `Open Weights=Yes` 候选；License 字段有空、other 和自定义许可；严格版本确认 0，全部权限状态 `not_assessed` | 队列只能称“元数据标记为开放权重”，不能宣称其评测版本均允许研究/复现；论文须明示尚未核实的许可范围 |

## 当前修订门禁

本轮的分层映射与许可证字段覆盖结果见 `Q4/08_历史遗留问题收口_20260926/`。分轨快照里的 `fit_executed=false` 只描述 v0.6 严格门控；当前已另有 C5/C6 分层探索拟合。C1 无条件 12/24 月趋势仍不可估计；C4 条件情景有数值预测但年度技能 `NOT_VALIDATED`、模拟带覆盖率 `UNKNOWN`；桥接映射有分层描述拟合但未校准，不能可靠转换 Q2 Loss。Loss–Benchmark 代码、结果与证据边界的独立范围 P2 已 PASS，复核和修订详情见 `Q4/08_历史遗留问题收口_20260926/q4_loss_bridge_final_audit_20260926.md`；该 PASS 不覆盖整个 Q4/08 修订包。冻结基线和原始附件未改。

### Loss–Benchmark 单家族探索补充 v0.9

对 C5/C6、B1、C1 和 7 份 Pythia 评测详情做了逐行对照：7 行高可比原始记录与配对审计一致；Loss 匹配 B1 最终 `steps=143000`、`D=299.893B`；考虑显示精度后 `exp(val_loss)` 与 PPL 舍入区间逐行相容。C5/C6 的六项分数与 C1 对齐，详情 JSON 中 5/7 行六项相符，Pythia-1b 和 6.9b 的 MATH 项存在差异；原始 C1 分数未被详情 JSON 替换。C1 与 B1/桥接表的参数量字段 7/7 有待解释的差异，名称连接不等于精确 checkpoint 证明。

单家族 OLS 关系为 `LB_Average = 7.671652 - 0.881664 × Val_Loss`，`R²=0.158`。按尺寸留一的 MAE/RMSE 为 0.478/0.553，留一均值基线为 0.376/0.424；因此没有证据表明线性映射具有预测优势。Q4/08 后续补充 C5 Medium 的 `48.929269 − 14.528726 × Val_Loss` 和 C6 Medium 的 `61.433677 − 19.184607 × Val_Loss`，分别按发布者留出：C5 未胜基线；C6 仅小幅胜出且 MAE 仍约 9.5 分。分层关系可用于报告 Loss/Benchmark 探索关联和误差，但不足以作为通用校准或 Q2→Benchmark 预测器。Task 3 仍直接使用 C1 原始分数，不把 Loss 桥误差混入条件情景。该结果仍为描述性探索，没有可迁移校准区间或未来预测支持，不与 C1 原始分数 Task 3 混用；v0.6 `BLOCKED_NO_FIT` 保留为历史门控状态。

复现入口 `scripts/q4_loss_benchmark_exploratory_mapping.py`；合同、crosswalk、拟合诊断、Q2 目标支持表、报告和 SHA-256 manifest 位于 `q4_loss_bridge_exploratory_contract_v0.9.md` 及 `results/bridge_exploratory/`。

v0.9 探索支线输出哈希已核对。经本轮修正，Q4/06 manifest 分别绑定 C5/C6 并验证 C5 43 行完整包含于 C6 75 行，新增 32 行；Q4/08 按发布者留出的组数列现命名为 `n_publisher_groups`。两条生成命令均退出码 0，29 项输入/代码/合同/输出哈希全匹配；独立 P2 复核确认拟合和留出指标不变。修订包继续保持 `revision_in_progress`，等待队伍审阅。

补充核对 C4 年度字段：2024 年 949 条记录中，训练算力/数据量标量有效数为 310/224；中位数为 `5.041×10^22 FLOP` / `2×10^11 tokens`，P90 为 `2.366×10^24 FLOP` / `1×10^13 tokens`。Open Weights Yes/No/unknown 分别为 405/294/250。C4 精确模型名与 C1 交集为 0，故这些数值只作年度背景。

1. 已只读历史结果表新增 BBH 逐任务敏感性分析；旧包输入、模型代码和主结果未改。
2. 最小烟雾运行与独立 P1 已 PASS（40 个模型覆盖 24 个任务；最大指数重建误差 `1.42×10^-14`）。
3. 全量运行已完成（789 个模型、24 个任务），结果与输入哈希记于 `results/full/q4_bbh_task_influence_manifest.json`。
4. 补充分析的全量独立 P2 已 PASS；审核证据见 `q4_bbh_task_review.md`。修订版仍待队伍审阅，保持 `revision_in_progress`，不执行冻结。

## Task 3 条件预测续作

用户明确要求完成题目规定的前沿预测模型、三种算力情景和模型代码。为保持历史判定可追溯，新增 `q4_task3_forecast_amendment_v0.8.md` 仅覆盖条件情景输出的留空规则；v0.6 冻结结果、v0.7 对年度技能不足的判定及 Loss 桥门控均保留。入口 `scripts/q4_frontier_scenario_model.py`。Q4/06 这一轮回测记录为：持平 MAE 0.7565（5 起点）、线性趋势 3.5961（5 起点）、AR(1) 0.8207（2 起点）、规模—时间模型 1.3047（5 起点）；这些是本目录该轮结果。后续 Q4/07 最终报告的一步 MAE 更新为 1.263，持平基线为 0.757，当前引用以 Q4/07 为准，两个版本不混用。条件情景结果只作为题目要求的情景生成器，不宣称预测优势。完整情景表、报告和 manifest 已写入本目录的 `results/forecast/`；每种情景/期限模拟 12,000 次，12 行点值/分位数端点在 0–100，哈希核验无不匹配。模拟带覆盖率未知；系数与前沿分别 bootstrap，未传播二者协方差。Task 3 独立 P1 与全量 P2 均 PASS。剩余限制是年度技能未验证、模拟带假设强，以及预测原点遵循题目设定的 2025-03。
