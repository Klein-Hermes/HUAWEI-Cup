# BBH 逐任务敏感性分析：独立复核回执

## P1 最小可运行结果

- **状态：PASS**。独立复核者在隔离输出目录重跑 40 个模型的烟雾切片；退出码 0，覆盖 24 个 BBH `acc_norm` 任务。
- 六族指数等于六个逐族百分位的等权平均，最大重建差 `1.42×10^-14`；逐项剔除排名、Top-N 指标与输出表复算一致。
- 修正了烟雾样本排名列的动态 Top-N 命名后，独立复核者再次复跑并确认 manifest 与文件哈希一致。
- P0/P1/P2：无发现。

## P2 补充分析终检

- **范围：** 仅审计本目录的 BBH 逐任务敏感性补充，不替代整篇论文 W2、submission QA，也不重新声明旧版 Q4 工作包的全部门禁。
- **状态：PASS**。独立复核者在隔离临时输出目录重跑全量命令，退出码 0；结果 CSV/Markdown 与本目录逐字节一致，manifest 输出哈希匹配（运行时间字段除外）。
- 独立确认 789 个唯一的六族完整模型、每个模型 24 项 BBH `acc_norm` 分数，共 18,936 个模型-任务单元；六族指数重建最大差 `2.84×10^-14`。
- 独立重算全部逐任务剔除结果及逐模型排名稳定性，所有字段零差异。核验结果包括：最小 Top-100 保留率 99.0%，最大名次位移 13，最大六族指数绝对变化 0.5719 点。
- 量纲与范围检查通过：原始 `acc_norm` 位于 0–1；百分位指数位于 0–100；Spearman 系数及保留率范围合理。报告没有把敏感性分析写成抽样不确定性、因果结论或版本确认。
- 历史 Q4 输入、结果目录和归档基线未被修改；本补充 manifest 的三个输入哈希与历史包一致。
- P0/P1/P2：无发现。

## 当前状态

分析产物和独立门禁通过，但 Q4 扩展版仍为 `revision_in_progress`。旧版内部包的冻结记录保留为历史状态；本回执不等于队伍已审阅修订版或整篇竞赛论文已就绪。

