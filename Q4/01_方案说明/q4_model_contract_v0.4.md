# Q4 建模合同 v0.4（暂定执行）

**状态：** 用户已确认沿用暂定队列和六族指标，授权继续推进；本合同与分析仍是队伍须复核的工作稿，不代表正式冻结。模型选择器显示的确切名称、对应模型版本及发布日期按用户指示留空。

## 1. 题目目标与输出

Q4 需形成三类结果：

1. 用模型能力的时间序列动态项和横截面组成分解分析能力变化；分解量为描述性关联，除非另有可审查的识别设计，不称因果效应。
2. 依据 C5/C6 或可核验文献，检查交叉熵 Loss 到明确定义的 Benchmark 原始分数是否可桥接，并报告门控、支持范围和验证误差。
3. 从附件中最后一个经确认可比的时间点讨论算力增长放缓下的 12/24 月前沿；数据不能支持时明确报告不可可靠估计，不补造预测值或区间。

每一项都需保存来源、筛选、单位、代码入口、结果与限制。C8 逐任务结果仍须覆盖，不能只展示汇总 Average。

## 2. 暂定样本、指标与 C1–C4 使用路径

- C1 是排行榜模型及表现主来源；C2 只补充元数据。名称候选没有 revision/SHA 证据时标为 `version_unresolved`，不能将名称相同说成同一版本。
- 综合能力指数采用六族：BBH、GPQA、IFEval、MATH Hard v2.0、MMLU-Pro、MuSR。先在每个叶任务上按有效候选计算 0–100 百分位秩，再族内叶任务等权、族间等权。指数是候选队列内相对位置，不是绝对成功率。IFEval 主指标为 `inst_level_strict_acc`。ARC 在与六族队列无共同支持时单独报告。
- C1 时间轴用 `Submission Date`，只解释为排行榜提交时间。主时间序列为每月新提交且 C2 明确 `Open Weights=Yes` 的 C1 `Average` 第 90 百分位；C1 License 空值和其他未知许可仍为 unknown。
- **C3 路径：** 读取 `leaderboard_extended_timeseries.csv`，只保留 `Source=Open LLM Leaderboard`。按 `(Year, Model)` 去重，逐年报告模型数、`Average` 中位数和 P90；完整年趋势主表截止 2024 年，2025 只作为未满年度覆盖信息，不并入完整年度变化率。`Historical (papers/reports)` 单列，不与排行榜数据拼接。C3 是独立年度背景序列，不按名称强行并入 C1/C8 个体记录，也不用于扩充 C1 的 12/24 月回测起点。
- **C4 路径：** 读取 `epoch_all_ai_models.csv`，以 `Publication date` 截止于 C8 最新主分析日期 2025-03-14，排除未来记录以避免前视信息。解析 `Training compute (FLOP)`、`Training dataset size (total)`、`Parameters`、`Open model weights?`，同时保留原始值、Confidence、Numerical format、Training compute estimation method 和日期。按出版年报告字段有效数、计算量中位数/P90、数据量中位数/P90及开放权重 Yes/No/unknown 数；不把缺失或区间值默认为精确值。C4 与 C1 精确名称交集为 0，因此只作年度宏观背景，不声称是 C1 候选的个体训练算力，不用于拟合能力—算力系数。
- 对 C3/C4 逐年表报告覆盖和缺失；某年有效记录不足以计算统计量时留空并给出原因。若来源字段定义、日期或数值精度不能核验，保留原始分类并降级为描述统计。

## 3. 动态能力模型与规模/非规模分解

### 3.1 动态模型

令 (F_t) 为第 2 节定义的月度开放权重前沿 P90（C1 `Average` 原量尺），只在连续且有观测的月份上估计一阶自回归描述模型：

\[
F_t = a + \rho F_{t-1} + u_t.
\]

这一定义将前一期能力水平显式放入本期方程，属于单变量动态模型；它只描述排行榜前沿的时间依赖，不代表因果机制。报告 (a,\rho) 点估计与 HC3 区间（区间只对异方差稳健、不校正序列相关），样本月数和残差诊断；若残差仍有序列相关，区间只作描述，不作正式推断。另以扩展窗口的一步预测和“沿用上月值”基线比较 MAE。只有完整连续月份不少于 8 个且设计矩阵满秩时才估计（至少 7 个转移、2 个系数、5 个残差自由度）；否则输出 `not_estimable`。无论是否能估计 AR(1)，年度预测仍须通过预测门：至少存在 3 个独立年度滚动起点，且回测 MAE 不高于预先定义的上月值基线，才允许给数值年度外推及区间；否则保持不可可靠估计。小样本下模型系数只作动态描述，不据此宣称稳定预测能力。

### 3.2 规模与其他组成的精确账面分解

沿用六族指数 (Y_i)，在有共同支持的模型记录上拟合描述性线性模型：

\[
\widehat Y_i=\hat\alpha+\hat\beta x_i+\hat{\boldsymbol\theta}^{\mathsf T}T_i+\hat\lambda_{m(i)},
\qquad x_i=\log_{10}(N_i\text{，单位为十亿参数}).
\]

早期与晚期各取预先指定的 C1 Submission 月（当前候选为 2024-11 与 2025-03），先限制到两期 (x_i) 的重叠区间。令 (\bar x_g) 为时期 (g\) 的平均 (x)，(\bar T_g) 为该时期类型指示变量均值，(\bar\lambda_g) 为该时期月份系数。用同一组已拟合系数定义两期预测均值差：

\[
\Delta_{\rm pred}=\hat\beta(\bar x_L-\bar x_E)
+\hat{\boldsymbol\theta}^{\mathsf T}(\bar T_L-\bar T_E)
+(\bar\lambda_L-\bar\lambda_E).
\]

因此规模组成项为第一项；非规模组成项为类型项与月份项之和；另分别列出类型项、月份项以及观测均值变化减去 (\Delta_{\rm pred}) 的剩余项。三项按定义严格加总为预测均值差；不再使用未定义的 Shapley 反事实。置信区间采用固定早/晚期经验协变量分布条件下、基于同一个 HC3 协方差矩阵的线性对比区间；不将其解释为抽样到更大模型总体的不确定性。

只有满足共同支持、两期各有正数记录、回归设计满秩且剩余自由度为正时才报告分解。贡献占比仅在总变化的 95% 区间不含 0、规模/非规模分量符号稳定且共同支持成立时计算；否则只报告指数点和区间。参数系数与时间/类型项均为观察性关联，非规模剩余项不能称为纯技术进步或因果贡献。

## 4. Loss–Benchmark 可行性门控（任何拟合之前）

预拟合门控脚本只读取、核对、去重并计算覆盖/支持统计，不拟合、不调参。它只判断是否允许进入后续建模分支，不提前宣称校准有效。每项保存原始行键、来源、审计状态和拒绝原因：

1. 核验 C5/C6 字段、原始单位、来源、Loss 与目标 Benchmark 是否均为有限数值；核验 `LB_Average` 与逐族 `LB_*` 的定义、量尺和测试集。
2. 以来源、模型标识、参数量、D、Loss 与所有 Benchmark 值组成内容键，识别 C5/C6 完全重复底层观测；重复只计一次。不同名称但不能确认版本的记录不合并。
3. 主要分析单位是独立模型族。逐行保留 `Loss_Comparability`、版本证据、验证集协议、目标指标定义和族标识；同一族的多尺寸模型可用于族内曲线，但不计作跨族独立验证。
4. 审计 Q1–Q3 中拟被转换的 Loss 来源、单位与实测目标支持范围。目标 Loss 不在通过核验的高可比样本范围内时，不允许靠模型外推生成桥接分数。C8 百分位指数不是 Benchmark 原始分数，不可直接作桥接响应。

预拟合门控状态唯一按以下规则决定：

- `BLOCKED_NO_FIT`：满足全部关键条件的高可比去重模型少于 3 个，或不同 Loss 值少于 3 个（不足以拟合含截距的一元线性关系并保留残差自由度）；或 Loss/Benchmark 单位、评测定义、目标来源任一无法核验；或目标 Loss 超出高可比实测范围。**不拟合。** 可查正式文献关系；若仍无可核验映射则报告不可桥接。
- `ELIGIBLE_LIMITED_EXPLORATORY`：上述单位、协议、目标来源及支持范围均通过，至少有 3 个不同 Loss 值的高可比去重模型，但独立可核验模型族少于 4 个。只允许拟合一个线性低自由度关系；只报告样本内探索结果，不报告可迁移校准误差或跨族置信区间。按模型族留一只作敏感性；模型行 bootstrap 不可替代跨族不确定性。
- `ELIGIBLE_FOR_CALIBRATION_VALIDATION`：至少有 4 个高可比去重模型且来自至少 4 个可核验独立模型族，目标 Loss 在实测范围内；并且对每个预定留一族训练折，训练集中有至少 3 个不同 Loss 值、设计矩阵满秩、测试 Loss 落在训练 Loss 范围内。此状态只允许进行最低限度线性拟合与留一族验证，**本身不等于支持校准**。

预拟合门控完成后，由 M1 审阅 CSV/报告及规则执行情况。只有 M1 通过且状态为两个 `ELIGIBLE_*` 之一，才运行相应最小拟合；P1 在该最小实现和留一验证后审查实现及数值。预先定义最终校准结论：对 `ELIGIBLE_FOR_CALIBRATION_VALIDATION`，所有留一族折都须产生有限预测，且汇总 LOFO MAE 不高于“各训练折目标均值预测”基线 MAE，才记为 `SUPPORTED_FOR_CALIBRATION`；否则降级为 `LIMITED_EXPLORATORY`，不声称迁移有效。任一必要折因奇异设计/缺少支持而无法估计时，不得记为支持校准。`ELIGIBLE_LIMITED_EXPLORATORY` 的结果保持探索性。若 `BLOCKED_NO_FIT`，拟合函数必须拒绝运行。

门控产物为 `q4_loss_benchmark_feasibility.csv` 与审计报告；最小模型实现/验证产物另存为 `q4_loss_benchmark_bridge_validation.csv`，不能把预拟合资格和最终校准状态合并成同一个阶段结论。

## 5. 预测与算力情景

- 预测原点暂定为开放权重 C1 前沿最后一个经确认可比月份，当前为 2025-03；目标为 2026-03 和 2027-03。前沿定义见第 2 节。
- 12/24 月预测需同时有连续的同源月度表现序列、年度滚动验证起点、来源可核验的算力情景输入，以及通过第 4 节的 Loss–Benchmark 桥接或其他预先验证的能力映射。任一门未过就不报告数值预测及统计区间。
- C3 年度 Open LLM Leaderboard 表、C4 截止 2025-03-14 的训练算力/数据量/开放权重字段分别形成背景序列；它们不与 C1 进行无证据的个体级连接。算力增长情景只有在行级定义、时间戳与数值精度可核验时才作为估计输入；否则只能作为明确标注的条件假设，不提升预测可信度。
- Q2 的 Loss 标度模型及 B9/B10 curated/estimated 外推不是实测前沿算力增长率，不能替代上述输入门。

## 6. 阶段门与文件

- 本版取代 v0.2/v0.3 中相冲突的分解、动态模型与门控规定；旧版保留为审核历史。`q4_input_contract_v0.1.md` 是早期草案。
- 当前 M1 对 v0.2 的固定回执为 FAIL；v0.3 复核发现预拟合门控和拟合后验证存在阶段循环。先对本 v0.4 独立 M1 复核并按意见修订；通过后才执行纯门控。门控结果须再次接受 M1 复核，后才做最小拟合与 P1；P1 通过才进入全量计算。P2 在所有结果、图、流程图与复现清单齐备之后。
- 输出只进入 Q4 的方案、代码、结果、图表和审计交付目录；题目、附件与 Q1–Q3 原始数据只读。

## 参考

- Shorrocks, A. F. “Decomposition procedures for distributional analysis: a unified framework based on the Shapley value.” *Journal of Economic Inequality* 11, 99–126 (2013). DOI: [10.1007/s10888-011-9214-z](https://doi.org/10.1007/s10888-011-9214-z)。该文不是本版线性账面分解的必要假设，也不提供本题数据的因果识别。
- 2026 年竞赛 AI 使用边界见[组委会规定](https://www.cmathc.org.cn/cpmcm/news/643.html)。
