# Q4 图表契约与 QA 记录

> AI 辅助说明：图表由 OpenAI Codex 桌面版 26.917.71314（build 10954；prod）辅助生成；模型 ID `gpt-6-luna`（GPT-6 Luna）；开发机构 OpenAI；发布日期 2026-09-22。

## 数据剖析

六族主指数完整样本 789 个；有正参数量的模型 788 个。参数量按 log10 展示。主指数先在每个叶任务内按唯一名称候选计算百分位秩，再做族内和族间等权。分布统计见 `../03_结果/results/q4_data_profile.csv`。

## 图表用途与口径

### raw_q4_data（原始数据候选；原始数据）

- 核心论点：展示主指数分布、参数规模分布及模型级规模—指数关系。
- 统计口径：参数直方图 n=788；指数直方图 n=789；散点 n=788；趋势线为未调整描述线。
- 版面：7.2 × 5.2 英寸；导出 SVG 与 300 dpi PNG，同时提供灰度预览。
- QA：轴标题、刻度和样本标注写入矢量 SVG；对应 PNG 已用 Microsoft YaHei 字体栅格化，之后由视觉复核确认。

### raw_q4_c3_coverage（原始数据候选；原始数据）

- 核心论点：显示 C3 排行榜和历史文献来源按年记录量不均。
- 统计口径：去冲突后的唯一模型记录数；横轴为 log10(n)；2025 标为部分年度覆盖；来源分列。
- 版面：7.2 × 5.2 英寸；导出 SVG 与 300 dpi PNG，同时提供灰度预览。
- QA：轴标题、刻度和样本标注写入矢量 SVG；对应 PNG 已用 Microsoft YaHei 字体栅格化，之后由视觉复核确认。

### raw_q4_loss_gate_inputs（原始数据候选；原始数据）

- 核心论点：呈现拟合前配对、版本、协议和目标范围候选的证据完整度。
- 统计口径：C5/C6 高可比去重记录、Q2 数值候选范围；协议五字段逐项计数，不拟合、不将范围重合解释为协议可比。
- 版面：7.2 × 5.2 英寸；导出 SVG 与 300 dpi PNG，同时提供灰度预览。
- QA：轴标题、刻度和样本标注写入矢量 SVG；对应 PNG 已用 Microsoft YaHei 字体栅格化，之后由视觉复核确认。

### process_q4_diagnostics（计算过程候选；计算过程）

- 核心论点：展示任务族覆盖、月×族样本支持和支持月份固定效应模型的个体残差。
- 统计口径：覆盖分母 1,819；月×族 n<20 的单元不着色；残差来自支持月份 HC3 模型，n=783。
- 版面：7.2 × 5.2 英寸；导出 SVG 与 300 dpi PNG，同时提供灰度预览。
- QA：轴标题、刻度和样本标注写入矢量 SVG；对应 PNG 已用 Microsoft YaHei 字体栅格化，之后由视觉复核确认。

### process_q4_version_match（计算过程候选；计算过程）

- 核心论点：展示从精确名称候选到严格版本确认的匹配损失。
- 统计口径：1,860 个 C8 最新目录；1,819 个唯一名称候选；严格版本确认 0；C4 精确名称交集 0。
- 版面：7.2 × 5.2 英寸；导出 SVG 与 300 dpi PNG，同时提供灰度预览。
- QA：轴标题、刻度和样本标注写入矢量 SVG；对应 PNG 已用 Microsoft YaHei 字体栅格化，之后由视觉复核确认。

### process_q4_ar1_backtest（计算过程候选；计算过程）

- 核心论点：逐点对照两个月留出时 AR(1) 与持平基线的一步绝对误差。
- 统计口径：目标月为 2025-02 与 2025-03；只显示两个扩展窗口起点，不画显著性或外推区间。
- 版面：7.2 × 5.2 英寸；导出 SVG 与 300 dpi PNG，同时提供灰度预览。
- QA：轴标题、刻度和样本标注写入矢量 SVG；对应 PNG 已用 Microsoft YaHei 字体栅格化，之后由视觉复核确认。

### result_q4_frontier（结果候选；分析结果）

- 核心论点：概览完整样本的任务族分布、历史月度描述分解和开放权重模型的月度进入表现。
- 统计口径：箱线须=P5/P95、箱=P25/P75、中位线=P50；开放前沿为每月 C1 Average P90，n<10 空心标注。
- 版面：7.2 × 5.2 英寸；导出 SVG 与 300 dpi PNG，同时提供灰度预览。
- QA：轴标题、刻度和样本标注写入矢量 SVG；对应 PNG 已用 Microsoft YaHei 字体栅格化，之后由视觉复核确认。

### result_q4_dynamic_decomposition（结果候选；分析结果）

- 核心论点：量化共同参数支持区间内各变化分量及观测变化的 HC3 区间。
- 统计口径：全量共同支持拟合 n=645、端点 n=92/110；rho 区间为描述性 HC3 区间；一步回测各 2 个起点；年度起点 0；Loss–Benchmark 保持 BLOCKED_NO_FIT。
- 版面：7.2 × 5.2 英寸；导出 SVG 与 300 dpi PNG，同时提供灰度预览。
- QA：轴标题、刻度和样本标注写入矢量 SVG；对应 PNG 已用 Microsoft YaHei 字体栅格化，之后由视觉复核确认。

### result_q4_c3_context（结果候选；分析结果）

- 核心论点：给出按来源分开的 C3 Average 中位数与 P90 年度背景。
- 统计口径：点为来源/年份内统计量，无跨年连线；标注每组记录数；历史文献数据不并入排行榜趋势，2025 OLLB 为部分年度。
- 版面：7.2 × 5.2 英寸；导出 SVG 与 300 dpi PNG，同时提供灰度预览。
- QA：轴标题、刻度和样本标注写入矢量 SVG；对应 PNG 已用 Microsoft YaHei 字体栅格化，之后由视觉复核确认。

### flow_overall_model（独立流程图；总体流程图）

- 核心论点：说明 Q4 从输入审计到预拟合门控、描述分解和结果封装的执行依赖。
- 统计口径：Loss–Benchmark 门控位于任何桥接拟合之前；当前阻断后不产生拟合；年度预测因无年度起点而不估计。
- 版面：7.2 × 5.2 英寸；导出 SVG 与 300 dpi PNG，同时提供灰度预览。
- QA：轴标题、刻度和样本标注写入矢量 SVG；对应 PNG 已用 Microsoft YaHei 字体栅格化，之后由视觉复核确认。

## 候选图覆盖

按数学建模图表技能，当前为 Q4 子问题准备原始数据、计算过程、分析结果三类各 3 张候选图；总体流程图单独列示，不计入 9 张候选。全部候选图覆盖 `q4`。候选用于论证与审阅，最终论文只保留有独立证据作用的图。

| 类别 | 候选文件 | 各自回答的问题 |
|---|---|---|
| 原始数据 | `raw_q4_data` / `raw_q4_c3_coverage` / `raw_q4_loss_gate_inputs` | 指数与参数分布；C3 来源/年份覆盖；Loss–Benchmark 拟合前证据可用性 |
| 计算过程 | `process_q4_diagnostics` / `process_q4_version_match` / `process_q4_ar1_backtest` | 覆盖与残差；名称到版本匹配流失；两次一步留出误差 |
| 分析结果 | `result_q4_frontier` / `result_q4_dynamic_decomposition` / `result_q4_c3_context` | 主指数和前沿；精确共同支持分解；来源分列的 C3 年度背景 |

总体流程图：`flow_overall_model`。C4 年度数据保留在 C4 年表，不绘制连续趋势，以避免把不同记录数和非精确版本匹配误读为共同时间序列。

## 已知呈现限制

当前绑定 Python 环境未安装 Matplotlib，图表使用 Pillow 与直接生成的 SVG 原语。Pillow 栅格输出与 SVG 共用同一绘图坐标和数据值。

开放前沿图的 P90 是月度新提交模型的截面统计，不是累计可用模型库的前沿；月样本不足 10 时标为空心点。所有图均为描述性图，不显示抽样置信区间或显著性符号。

result_q4_frontier 中的月度恒等项为旧版月均值描述分解；精确共同支持分解及 HC3 区间以 `result_q4_dynamic_decomposition` 和 `q4_scale_tech_contribution.csv` 为准。算术闭合误差由端点月份固定效应和 OLS 正规方程导致，仅作数值核对，不表示存在未解释因素。
